---
phase: 02-contact-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apollo/client.py
  - tests/test_apollo_client.py
autonomous: true
requirements: [DISC-03]

must_haves:
  truths:
    - "search_people returns (people_list, 'OK') on 200 and (None, plain-language banner) on 401/403/429/network"
    - "bulk_match_people returns verified-email matches on 200 and surfaces Apollo's own 422 message"
    - "enrich_candidates batches a 25-candidate list into 3 bulk_match calls, each with <=10 details"
    - "A 429 triggers exponential backoff, max 3 attempts, banner only after all exhausted"
    - "The api_key is only ever sent in the x-api-key header, never logged or printed"
  artifacts:
    - path: "apollo/client.py"
      provides: "search_people, bulk_match_people, enrich_candidates, _post_with_retry (extends existing typed-tuple client)"
      exports: ["search_people", "bulk_match_people", "enrich_candidates"]
    - path: "tests/test_apollo_client.py"
      provides: "Unit coverage for DISC-03 + 401/403/422/429 error handling + batching"
      contains: "test_bulk_match_people_batches_of_ten"
  key_links:
    - from: "enrich_candidates"
      to: "bulk_match_people"
      via: "chunked calls of <=10 details"
      pattern: "range\\(0, len\\(.*\\), 10\\)|_chunk"
    - from: "_post_with_retry"
      to: "429 backoff"
      via: "retry loop with time.sleep"
      pattern: "429"
---

<objective>
Extend `apollo/client.py` with the two credit-relevant Apollo calls — `search_people` (0 credits) and `bulk_match_people` (1 credit/match) — plus an `enrich_candidates` batching wrapper (≤10 per call) and a shared `_post_with_retry` helper implementing the CLAUDE.md-mandated 429 exponential backoff (max 3 retries), which does not yet exist anywhere in the codebase. All new functions copy the existing typed-tuple, never-raise, defensive-`.get()` shape and are covered by RED-first mocked-`requests` tests that go green in this plan.

Purpose: These are the only two Apollo network calls this phase makes. Isolating them behind the established client contract keeps the discovery page a thin composition layer and makes DISC-03 + all error codes provable with mocked `requests` in ~10s.
Output: extended `apollo/client.py`, extended `tests/test_apollo_client.py`, full suite green.
</objective>

## Phase Goal

**As a** non-technical student-org teammate, **I want to** pick an outreach path and describe who I'm looking for in plain words, **so that** I get a deduped list of contacts with verified emails without configuring any filters or spending credits blindly.

<execution_context>
@/Users/yashpersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yashpersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-contact-discovery/02-RESEARCH.md
@.planning/phases/02-contact-discovery/02-PATTERNS.md

<interfaces>
<!-- Existing client contract to extend verbatim — read apollo/client.py before editing. -->

From apollo/client.py (existing):
```python
APOLLO_BASE = "https://api.apollo.io/api/v1"
def check_apollo_health(api_key: str) -> tuple[bool, str]:  # try/except requests.RequestException,
    # then explicit 401 -> 403 -> != 200 branches, then defensive data.get(...) parse
```
New endpoints (request/response field names are MEDIUM confidence — use .get() everywhere; Plan 03 confirms live):
  POST /mixed_people/api_search  body {"person_titles":[...], "q_organization_keyword_tags":[...], "per_page":100, "page":1}
    -> {"people": [ {"id","first_name","last_name_obfuscated","title","has_email",
                     "organization":{"name","website_url"}} ], "total_entries": N}  (0 credits)
  POST /people/bulk_match  body {"details":[ {"id","first_name","last_name","organization_name","domain"} ]}  (max 10 details)
    -> {"matches":[ {"id","first_name","last_name","name","email","email_status","title",
                     "organization_name","credits_consumed"} ]}  (1 credit per matched person)

mock_requests_response(status_code, json_data) fixture already exists in tests/conftest.py.
New functions use requests.post (not requests.get) — monkeypatch requests.post in tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED tests for search_people, bulk_match_people, enrich_candidates</name>
  <files>tests/test_apollo_client.py</files>
  <read_first>
    - tests/test_apollo_client.py (existing test_check_apollo_health — copy its shape exactly)
    - tests/conftest.py (mock_requests_response fixture — reuse, do not reimplement)
    - .planning/phases/02-contact-discovery/02-RESEARCH.md (Pattern 1, Pattern 3, Pitfall 2, Pitfall 3)
    - .planning/phases/02-contact-discovery/02-UI-SPEC.md (Copywriting Contract — exact error banner strings per code)
  </read_first>
  <action>
    Extend `tests/test_apollo_client.py` (keep the import-inside-body / RED convention already used there; `monkeypatch.setattr(requests, "post", ...)` since new functions POST). Add:
    (1) `test_search_people_error_codes` — 401 → `(None, msg)`, 403 → `(None, msg)`, 429 (all retries exhausted) → `(None, msg)`, and 200 with `{"people":[...]}` → `(list, "OK")`; each `msg` is a non-empty str; assert the 200 case returns the `people` list defensively even if `total_entries` is absent.
    (2) `test_bulk_match_people_error_codes` — 401/403/429 → `(None, msg)`; 422 with `{"message":"..."}` → `(None, msg)` where the returned banner contains Apollo's own message text (UI-SPEC "Enrichment failure ... (422)"); 200 with `{"matches":[...]}` → `(list, "OK")`.
    (3) `test_bulk_match_people_batches_of_ten` — monkeypatch `requests.post` with a call-counter; feed `enrich_candidates` a 25-candidate list and assert exactly 3 POSTs were made and every call's `details` array had length ≤ 10 (DISC-03 batching, RESEARCH Pattern 3); assert each `details[]` object carries `id` AND name/org fields together (Pitfall 3 hedge), not id-only.
    (4) `test_search_people_never_raises_on_network` — monkeypatch `requests.post` to raise `requests.RequestException` and assert `search_people` returns `(None, non_empty_str)` rather than propagating.
    Write bodies per the existing test_check_apollo_health analog; no fenced impl in this plan file.
  </action>
  <verify>
    <automated>pytest tests/test_apollo_client.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_apollo_client.py --collect-only -q` lists the 4 new tests plus the existing test_check_apollo_health, no collection error
    - Running the file (no --collect-only) fails RED on the new tests (search_people / bulk_match_people / enrich_candidates unimplemented)
    - Every new test imports its target inside the test body and monkeypatches `requests.post`
    - The batching test asserts an exact call count of 3 for 25 candidates and per-call `len(details) <= 10`
  </acceptance_criteria>
  <done>test_apollo_client.py has 4 new RED tests that collect cleanly and fail pre-implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement search_people + _post_with_retry</name>
  <files>apollo/client.py</files>
  <read_first>
    - apollo/client.py (existing check_apollo_health / get_credit_balance — copy typed-tuple, status-branch, defensive-.get() shape)
    - tests/test_apollo_client.py (RED contract from Task 1)
    - .planning/phases/02-contact-discovery/02-RESEARCH.md (Pattern 1 search_people; Pitfall 2 the 429 backoff requirement)
  </read_first>
  <behavior>
    - search_people 200 -> (data.get("people", []), "OK")
    - 401 -> (None, "Apollo connection failed — check that APOLLO_API_KEY is set correctly in Streamlit secrets.")
    - 403 -> (None, "Apollo connection failed — this key is not a Master API key. Check Apollo Settings → Integrations → API Keys.")
    - 429 after 3 exhausted retries -> (None, "Apollo is rate-limiting search requests right now — please wait a moment and try again.")
    - network RequestException -> (None, "Contact search failed — check your internet connection and try again.")
  </behavior>
  <action>
    Update the module docstring to mention the new search/enrichment functions. Add a private `_post_with_retry(url, api_key, json_body, timeout=15, max_attempts=3)` returning a `requests.Response | None` (None only on a network `requests.RequestException` after retries): POST with header `{"x-api-key": api_key}`; on a 429 response, sleep an exponential backoff (`time.sleep`, e.g. 0.5·2^attempt) and retry up to `max_attempts`, returning the final 429 Response if still limited; return non-429 responses immediately. Import `time`. Add `search_people(api_key, person_titles, org_keyword_tags, per_page=100, page=1) -> tuple[list[dict] | None, str]` that builds the body per the interfaces block, calls `_post_with_retry` against `f"{APOLLO_BASE}/mixed_people/api_search"`, and branches exactly 401 → 403 → 429 → generic `!= 200` → success (`data.get("people", [])`, "OK"), reusing the verbatim banner strings from the behavior block. Never log/print `api_key` (T-04-02). Every response field via `.get()`.
  </action>
  <verify>
    <automated>pytest tests/test_apollo_client.py::test_search_people_error_codes tests/test_apollo_client.py::test_search_people_never_raises_on_network -q</automated>
  </verify>
  <acceptance_criteria>
    - The two search_people tests pass
    - `grep -n "def _post_with_retry" apollo/client.py` exists and the function contains a `429` branch with `time.sleep`
    - `grep -nE "print\(|log" apollo/client.py` shows no logging/printing of api_key
    - search_people parses via `.get("people"`, never `data["people"]`
  </acceptance_criteria>
  <done>search_people + _post_with_retry implemented; their tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement bulk_match_people + enrich_candidates batching</name>
  <files>apollo/client.py</files>
  <read_first>
    - apollo/client.py (search_people + _post_with_retry from Task 2 — reuse the helper)
    - tests/test_apollo_client.py (batching + 422 contract from Task 1)
    - .planning/phases/02-contact-discovery/02-RESEARCH.md (Pattern 3 batching + redundant-fields note; Pitfall 3)
    - .planning/phases/02-contact-discovery/02-UI-SPEC.md (Copywriting Contract 422 wording)
  </read_first>
  <behavior>
    - bulk_match_people 200 -> (data.get("matches", []), "OK")
    - 422 -> (None, "Apollo couldn't enrich these contacts — {response.json()['message']}") surfacing Apollo's own message
    - 401/403/429 -> (None, verbatim UI-SPEC banner for that code)
    - enrich_candidates(api_key, 25_candidates) -> exactly 3 bulk_match_people calls, details<=10 each, merged matches
    - enrich_candidates surfaces the first failing batch's (None, msg) immediately
  </behavior>
  <action>
    Add `bulk_match_people(api_key, details) -> tuple[list[dict] | None, str]`: POST `{"details": details}` to `f"{APOLLO_BASE}/people/bulk_match"` via `_post_with_retry`; branch 401 → 403 → 429 → 422 (surface `resp.json().get("message")` inside the exact UI-SPEC 422 banner, degrading gracefully if `message` is absent) → generic `!= 200` → success (`data.get("matches", [])`, "OK"). Add a private `_chunk(items, size=10)` generator and `enrich_candidates(api_key, candidates) -> tuple[list[dict] | None, str]`: iterate `_chunk(candidates, 10)`, build each `details[]` object with `id` PLUS `first_name`/`last_name`/`organization_name`/`domain` derived defensively from the candidate (Pitfall 3 hedge — never id-only), call `bulk_match_people` per chunk, and on the first `(None, msg)` return it immediately (D-06 fail-fast); otherwise extend an accumulator and return `(all_matches, "OK")`. Do NOT request `reveal_phone_number` (CLAUDE.md — +8 credits, out of scope). Every field via `.get()`.
  </action>
  <verify>
    <automated>pytest tests/test_apollo_client.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Full `tests/test_apollo_client.py` passes (all 4 new tests + existing health test)
    - `grep -n "reveal_phone_number" apollo/client.py` returns nothing
    - enrich_candidates chunks at size 10 (grep shows `_chunk` or `range(0, len(...), 10)`)
    - Each details object includes `id` and at least one of first_name/organization_name (grep confirms redundant fields, not id-only)
    - 422 branch references `.json().get("message")` and wraps it in the UI-SPEC banner text
  </acceptance_criteria>
  <done>bulk_match_people + enrich_candidates implemented; test_apollo_client.py fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Apollo API | Outbound REST with the master key in x-api-key header |
| Apollo response → parsing | MEDIUM-confidence field names, parsed defensively |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | Apollo 429 on search/enrichment | mitigate | `_post_with_retry` exponential backoff, max 3 attempts, banner only after all exhausted (Pitfall 2) |
| T-02-04 | Information Disclosure | APOLLO_API_KEY in headers/logs | mitigate | key sent only in x-api-key header; grep gate proves no print/log of key (T-04-02) |
| T-02-05 | Spoofing | 401/403 responses | mitigate | branch to plain-language banner, never retry a 401/403, never leak key material |
| T-02-11 | Information Disclosure | credit waste from id-only bulk_match no-matches | mitigate | pass redundant name/org/domain fields per Pitfall 3; Plan 03 confirms live match rate |
| T-02-SC | Tampering | pip installs | accept | Phase introduces ZERO new packages (RESEARCH Package Legitimacy Audit: N/A) — no install task exists |
</threat_model>

<verification>
- `pytest tests/test_apollo_client.py -q` fully green
- `pytest -q` full suite green (Phase 1 regressions guarded)
- grep gates pass (no key logging, no reveal_phone_number, batching ≤10, 429 backoff present)
</verification>

<success_criteria>
- search_people + bulk_match_people return correct typed tuples for 200/401/403/422/429/network (DISC-03 + error handling)
- enrich_candidates batches ≤10 per call and fails fast on the first batch error
- 429 backoff implemented (max 3 retries) — the first codebase implementation of retry logic
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/02-contact-discovery/02-02-SUMMARY.md` when done.
</output>
