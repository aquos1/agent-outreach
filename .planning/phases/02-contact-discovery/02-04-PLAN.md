---
phase: 02-contact-discovery
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - pages/discovery_page.py
  - app.py
autonomous: false
requirements: [PATH-01, PATH-03]

must_haves:
  truths:
    - "Teammate sees exactly three path options in order: Club Sponsorship, Productthon Sponsorship, Client Sourcing"
    - "Teammate enters company type + target role as free text; no raw Apollo filter UI is shown"
    - "Find Contacts (free) shows a count + cost estimate; a separate Enrich & Continue button spends credits"
    - "Zero results render as st.info (never a red/warning banner), with inputs still editable to retry"
    - "After enrichment, a read-only Name/Company/Title/Email table renders and rows are written to prospect as status='enriched'"
    - "The Discovery page is reachable from app navigation while Health remains the default landing page"
    - "D-06: Find Contacts (free) and Enrich & Continue are two distinct buttons/stages, not one combined action"
    - "D-07: the Find Contacts stage renders count + cost estimate only — no contact table until enrichment succeeds"
    - "D-10: the post-enrichment contact table is read-only — no per-contact removal/deselection UI"
    - "D-11: the results table renders exactly the columns Name, Company, Title, Email, no industry/company-size columns"
    - "D-12: the empty state renders as st.info with 'No new contacts found — try different search terms', never a warning/error, inputs stay editable"
  artifacts:
    - path: "pages/discovery_page.py"
      provides: "Two-stage session-state Find/Enrich discovery page wiring logic + client + prospects"
      min_lines: 60
    - path: "app.py"
      provides: "Discovery page registered in st.navigation pages dict"
      contains: "discovery_page.py"
  key_links:
    - from: "pages/discovery_page.py"
      to: "discovery.logic + apollo.client + db.prospects"
      via: "imports composed inside button blocks"
      pattern: "from discovery.logic import|from apollo.client import|from db.prospects import"
    - from: "app.py pages dict"
      to: "pages/discovery_page.py"
      via: "st.Page registration"
      pattern: "discovery_page.py"
    - from: "Enrich & Continue button"
      to: "prospect table"
      via: "insert_enriched(status='enriched')"
      pattern: "insert_enriched"
---

<objective>
Deliver the teammate-facing vertical slice: the Contact Discovery page that composes Plan 01's logic + Plan 02's Apollo client + Plan 01's dedup/insert into the two-stage, session-state-driven Find→Enrich flow, rendered exactly per the UI-SPEC, and register it in `app.py` navigation. This is the plan where a teammate can actually pick a path, describe who they want, see the credit cost before committing, and get a deduped list of verified-email contacts.

Purpose: Everything before this was shared, live-verified backend. This plan turns it into the usable capability the phase goal promises — and is the only plan that satisfies PATH-01 (three options) and PATH-03 (free-text inputs), both manual-verified per the UI-SPEC.
Output: `pages/discovery_page.py`, updated `app.py`, and a manual click-through confirmation.
</objective>

## Phase Goal

**As a** non-technical student-org teammate, **I want to** pick an outreach path and describe who I'm looking for in plain words, **so that** I get a deduped list of contacts with verified emails without configuring any filters or spending credits blindly.

<execution_context>
@/Users/yashpersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yashpersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-contact-discovery/02-CONTEXT.md
@.planning/phases/02-contact-discovery/02-UI-SPEC.md
@.planning/phases/02-contact-discovery/02-PATTERNS.md

<interfaces>
<!-- All contracts this page composes already exist after Plans 01-03. Read those files; do not re-explore. -->

From discovery/logic.py:
  PATH_OPTIONS: list[str]  # exactly ["Club Sponsorship","Productthon Sponsorship","Client Sourcing"]
  PATH_CONFIG: dict[str, dict]  # label -> {"slug": "club_sponsorship"|"productthon"|"client_sourcing"}
  CONTACT_CAP = 50
  build_search_filters(company_type, target_role) -> (person_titles, org_keyword_tags)  # path-free
  filter_has_email(people) -> list[dict]
  cost_estimate(candidates) -> int  # min(len, 50), no Apollo call

From apollo/client.py:
  search_people(api_key, person_titles, org_keyword_tags, per_page=100, page=1) -> (list|None, str)
  enrich_candidates(api_key, candidates) -> (list|None, str)  # batches <=10, matches carry name/email/title/organization_name

From db/prospects.py:
  dedup_filter(candidates, db_path="db/outreach.db") -> list[dict]
  insert_enriched(rows, path, db_path="db/outreach.db") -> int

From app.py (existing extension point, lines ~38-42):
  pages = {"Health": [st.Page("pages/health_page.py", title="System Health", default=True)],
           # Phase 2+ will append real pages here ...}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the two-stage discovery page</name>
  <files>pages/discovery_page.py</files>
  <read_first>
    - pages/health_page.py (st.secrets read, try/except-wrap-every-external-call, st.spinner, st.success/st.error/st.info icon conventions — copy these; note it single-passes, discovery is button-gated)
    - .planning/phases/02-contact-discovery/02-UI-SPEC.md (Layout & Discovery Flow State System table + Copywriting Contract — AUTHORITATIVE for copy/icons/state transitions)
    - .planning/phases/02-contact-discovery/02-RESEARCH.md (Pattern 2 session-state two-stage flow; Pitfall 4 pipeline ordering; Pitfall 5 button-gating)
    - discovery/logic.py, apollo/client.py, db/prospects.py (the functions this page composes)
  </read_first>
  <action>
    Create `pages/discovery_page.py` starting with `from __future__ import annotations`, `import streamlit as st`, and imports of the functions listed in the interfaces block. Read `apollo_key = st.secrets.get("APOLLO_API_KEY")` once at top (never re-validate — the app.py/health gate already guarantees a valid key); never log/print it. Render `st.title("Contact Discovery")` + the exact UI-SPEC caption. Initialize `st.session_state.find_result` and `st.session_state.enrich_result` to None if absent. Render, in order: `st.selectbox("Outreach path", PATH_OPTIONS, index=None, placeholder="Select an outreach path...")` (exactly 3 options, PATH-01); two `st.text_input`s "Company type or industry" and "Target role" with the exact UI-SPEC placeholders + helper caption (PATH-03); a `st.button("Find Contacts", type="primary", icon=":material/manage_search:", disabled=<no path OR both text fields empty>)`.
    Stage 1 (inside `if find button:`): reset `enrich_result=None`; wrap the pipeline in `st.spinner("Searching Apollo for matching contacts...")` and try/except-to-banner: `build_search_filters` → `search_people` (on `(None,msg)` render `st.error(msg, icon=":material/cancel:")` and stop this branch, NOT the page) → `filter_has_email` (DISC-02) → `dedup_filter` (DEDUP-02) → cap to `CONTACT_CAP` → compute `cost_estimate` LAST (Pitfall 4 ordering). Store `{"candidates": [...], "cost": n}` in `st.session_state.find_result`. Render from session state: if candidates non-empty, `st.info` with the exact count+cost copy (use the >50-capped wording when the pre-cap match count exceeded 50), icon `:material/manage_search:`; if zero, `st.info("No new contacts found — try different search terms.", icon=":material/search_off:")` — NEVER st.warning/st.error (D-12).
    Stage 2 (only when `find_result` has candidates): render `st.button("Enrich & Continue", type="primary", icon=":material/bolt:")`; inside its block, `st.spinner("Enriching contacts with verified emails...")` + try/except: `enrich_candidates(apollo_key, candidates[:CONTACT_CAP])` (on `(None,msg)` render `st.error(msg, icon=":material/cancel:")`, keep inputs+buttons usable, do NOT st.stop) → on success `insert_enriched(matches, path=PATH_CONFIG[selected]["slug"])` → store rows in `st.session_state.enrich_result`. Render from session state: `st.success("Enrichment complete — {n} contact(s) ready.", icon=":material/check_circle:")` + `st.dataframe(rows)` with exactly the columns Name, Company, Title, Email in that order (map from match name/organization_name/title/email), read-only, no on_select (D-10/D-11/D-13). Gate ALL Apollo calls strictly inside the button blocks (Pitfall 5) — never at module top. Never call `st.stop()` on a failure (PATTERNS counter-example) — inputs and both buttons must stay usable to retry.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('pages/discovery_page.py').read()); print('parse-ok')"</automated>
    <human-check>Deferred to Task 3 manual click-through (no Streamlit component test harness in this project — VALIDATION.md manual-only rows).</human-check>
  </verify>
  <acceptance_criteria>
    - `python -c "import ast; ast.parse(open('pages/discovery_page.py').read())"` succeeds (valid module)
    - `grep -c "search_people\|enrich_candidates" pages/discovery_page.py` > 0 and every such call sits inside an `if st.button(...)` block (Apollo never called at module top — Pitfall 5)
    - The zero-results branch uses `st.info` with `:material/search_off:` and contains NO `st.warning`/`st.error` for the empty state (grep the empty-state copy line)
    - `st.dataframe` is present with Name/Company/Title/Email columns; no `st.table`/custom HTML table
    - `grep -n "st.stop()" pages/discovery_page.py` returns nothing
    - `st.selectbox` uses `index=None` and PATH_OPTIONS (exactly 3 options)
  </acceptance_criteria>
  <done>pages/discovery_page.py implements the full two-stage session-state Find→Enrich flow per the UI-SPEC and parses cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Register the Discovery page in app navigation</name>
  <files>app.py</files>
  <read_first>
    - app.py (existing pages dict + comment placeholder at lines ~38-42; do NOT touch the schema bootstrap or secrets gate above it)
    - .planning/phases/02-contact-discovery/02-PATTERNS.md (section "app.py (EXTEND)" — exact insertion point)
  </read_first>
  <action>
    In `app.py`, replace the `# Phase 2+ will append real pages here ...` comment inside the `pages` dict with a new key `"Discovery": [st.Page("pages/discovery_page.py", title="Contact Discovery")]`. Keep `"Health"` as `default=True` — Health remains the boot-time gate/landing page (CLAUDE.md page list). Do NOT modify `ensure_schema()` (line ~22), the secrets-presence gate (lines ~26-34), or the `st.navigation(pages)` / `pg.run()` calls. No new imports needed.
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('app.py').read()); src=open('app.py').read(); sys.exit(0 if 'discovery_page.py' in src and 'default=True' in src else 1); " && echo nav-ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "discovery_page.py" app.py` == 1
    - `grep -c "default=True" app.py` == 1 and it is on the Health page line (Health still default)
    - The schema bootstrap and secrets gate lines are unchanged (ensure_schema() and the APOLLO_API_KEY st.error/st.stop block still present)
    - `python -c "import ast; ast.parse(open('app.py').read())"` succeeds
  </acceptance_criteria>
  <done>app.py registers the Discovery page; Health stays the default landing page; boot infrastructure untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual click-through of the discovery flow</name>
  <action>Run the app and manually click through the discovery flow (path selection, free-text entry, Find, empty state, Enrich, results table, DB check) to confirm PATH-01/PATH-03 and the two-stage session-state flow render per the UI-SPEC. This is a human verification step — see how-to-verify for the exact steps.</action>
  <what-built>
    The Contact Discovery page and its navigation entry. This checkpoint confirms PATH-01, PATH-03, and the two-stage session-state flow, which have no automated Streamlit test harness in this project (VALIDATION.md manual-only rows).
  </what-built>
  <how-to-verify>
    1. Run `streamlit run app.py`. Confirm the app boots to System Health (still default) and a "Contact Discovery" entry appears in the sidebar navigation.
    2. Open Contact Discovery. Confirm exactly THREE path options in order: "Club Sponsorship", "Productthon Sponsorship", "Client Sourcing" (PATH-01), and that "Find Contacts" is disabled until a path is selected AND at least one free-text field is filled.
    3. Pick a path, enter a company type and target role (PATH-03), click "Find Contacts". Confirm you see a count + cost estimate `st.info` (e.g. "Found N new contacts — enriching will use up to N credits."), and that "Enrich & Continue" now appears. Confirm NO contact table is shown yet (D-07).
    4. Enter deliberately absurd search terms and click Find again. Confirm the empty state shows a neutral `st.info` ("No new contacts found — try different search terms."), NOT a red/warning banner, and the inputs remain editable (D-12).
    5. With a real match set, click "Enrich & Continue". Confirm the spinner, then a green "Enrichment complete — N contact(s) ready." and a read-only Name/Company/Title/Email table (D-11). Confirm re-clicking "Find Contacts" resets the table/enrichment (fresh search).
    6. Confirm the enriched rows landed in SQLite: `sqlite3 db/outreach.db "SELECT name,email,path,status FROM prospect WHERE status='enriched' LIMIT 5;"` shows rows with status='enriched'.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any deviation from the UI-SPEC (wrong copy, wrong option order, table shown too early, empty state styled as a warning, etc.).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teammate browser input → Streamlit server | Free-text company type/role crosses into Apollo query + parameterized SQL |
| Find (free) → Enrich (credits) | Credit-spend gate; must not spend without explicit second click |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | user free-text → Apollo array fields / SQLite | accept | Free text goes only to Apollo array params (0-credit, server-validated) and parameterized SQL in db/prospects.py; no injection path into local systems |
| T-02-08 | Denial of Service | Apollo re-queried on every Streamlit rerun | mitigate | all search_people/enrich_candidates calls gated inside `if st.button(...)` blocks; results persisted in st.session_state (Pitfall 5) |
| T-02-09 | Information Disclosure / Resource | credits spent without teammate consent | mitigate | two-stage gate — Find (free) shows cost estimate before the separate Enrich button spends credits (D-06) |
| T-02-04 | Information Disclosure | APOLLO_API_KEY on the page | mitigate | read via st.secrets, passed to client functions only; never st.write/print/logged |
| T-02-SC | Tampering | pip installs | accept | Phase introduces ZERO new packages (RESEARCH Package Legitimacy Audit: N/A) — no install task exists |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(...)"` parses both files
- grep gates: Apollo calls button-gated, empty state is st.info, st.dataframe present, no st.stop(), Health still default
- Manual click-through confirms PATH-01, PATH-03, two-stage flow, empty state, read-only table, and prospect rows written status='enriched'
- `pytest -q` full suite still green
</verification>

<success_criteria>
- Teammate sees exactly 3 paths and configures nothing else about targeting (PATH-01, PATH-02)
- Free-text company type + role drive an invisible Apollo search (PATH-03, DISC-01)
- Cost estimate shown before the separate credit-spending Enrich action (DISC-04, D-06)
- Enriched, deduped, verified-email contacts render read-only and persist as status='enriched' (DISC-02, DISC-03, DEDUP-02, D-10/D-11)
- Discovery page reachable in navigation; Health remains default
</success_criteria>

<output>
Create `.planning/phases/02-contact-discovery/02-04-SUMMARY.md` when done.
</output>
