---
phase: 02-contact-discovery
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - apollo/client.py
  - discovery/logic.py
autonomous: false
requirements: [DISC-02, DISC-03]

must_haves:
  truths:
    - "A real /mixed_people/api_search call returns people with the assumed field names (id, has_email, organization.website_url) — or the code is reconciled to the real names"
    - "A real /people/bulk_match call returns matches with email + email_status at a non-trivial match rate"
    - "Any field-name mismatch found live is corrected in discovery/logic.py and apollo/client.py .get() lookups"
    - "The full test suite stays green after any reconciliation"
  artifacts:
    - path: "apollo/client.py"
      provides: "Field-name lookups confirmed against live Apollo (or corrected)"
    - path: "discovery/logic.py"
      provides: "has_email / organization.website_url lookups confirmed against live Apollo (or corrected)"
  key_links:
    - from: "live search response"
      to: "filter_has_email + dedup_filter field lookups"
      via: "confirmed field names"
      pattern: "has_email"
---

<objective>
Resolve the MEDIUM-confidence Apollo response field names (RESEARCH Assumptions A1–A4) with one live authenticated `/mixed_people/api_search` call and one small live `/people/bulk_match` call, then reconcile any mismatches in the defensive `.get()` lookups before the discovery page is built on top of them. This exactly repeats Phase 1's successful pattern (01-04 Task 3 credit-balance field verification).

Purpose: The page (Plan 04) renders a Name/Company/Title/Email table and spends real credits — a silent field-name mismatch would waste credits and show an empty/wrong table. Confirming the shapes live, against real Apollo, before wiring the UI removes the single biggest risk in this phase.
Output: confirmed (or corrected) field lookups in `apollo/client.py` + `discovery/logic.py`; suite green.
</objective>

## Phase Goal

**As a** non-technical student-org teammate, **I want to** pick an outreach path and describe who I'm looking for in plain words, **so that** I get a deduped list of contacts with verified emails without configuring any filters or spending credits blindly.

<execution_context>
@/Users/yashpersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yashpersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-contact-discovery/02-RESEARCH.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
@apollo/client.py
@discovery/logic.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live Apollo field-name verification (one search + one small bulk_match)</name>
  <action>Run one live /mixed_people/api_search call and one small (<=3 people) live /people/bulk_match call against the real Apollo account; inspect the raw JSON and record which response field names match the assumptions and which differ. This is a human-driven verification, not an autonomous edit — see how-to-verify for the exact steps.</action>
  <what-built>
    Plans 01–02 built search_people, bulk_match_people, enrich_candidates, filter_has_email, and dedup_filter against Apollo field names that RESEARCH rates MEDIUM confidence (corroborated by secondary/tertiary sources, not a first-party JSON example). This checkpoint makes two real authenticated calls against the live Apollo account to confirm the actual response shapes before the UI is built on them.
  </what-built>
  <how-to-verify>
    1. Confirm `.streamlit/secrets.toml` has a real `APOLLO_API_KEY` (Master key). If absent, this task blocks until it is provided — same pause/resume as Phase 1 Task 3.
    2. From a Python shell in the repo root, run ONE live search:
       `from apollo.client import search_people; people, msg = search_people(<key>, ["Marketing Director"], ["software startups"], per_page=5, page=1); print(msg); import json; print(json.dumps(people[:2], indent=2))`
       Inspect the raw JSON. Confirm these keys exist per person (or record the real names if different): `id`, `has_email`, `first_name`, `title`, and `organization` containing `name` + `website_url` (or the real domain-bearing field).
    3. Run ONE small live bulk_match with AT MOST 3 real people from the search result (keep credit spend ≤3):
       build `details` with `id` + `first_name`/`last_name`/`organization_name`/`domain`, call `from apollo.client import bulk_match_people; matches, msg = bulk_match_people(<key>, details[:3]); print(msg); print(json.dumps(matches, indent=2))`.
       Confirm each match carries `email`, `email_status`, `name`, `organization_name`, `credits_consumed` (or record the real names). Confirm the match rate is non-trivial (not near-zero) with the redundant fields present (Pitfall 3).
    4. Record any field-name differences from the assumptions above; these feed Task 2.
  </how-to-verify>
  <resume-signal>Type "confirmed — fields match" OR paste the corrected field names observed live (which fields differ and their real names).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reconcile any field-name mismatches</name>
  <files>apollo/client.py, discovery/logic.py</files>
  <read_first>
    - apollo/client.py (search_people/bulk_match_people/enrich_candidates .get() lookups)
    - discovery/logic.py (filter_has_email lookup)
    - db/prospects.py (dedup_filter domain lookup + insert_enriched field derivation)
    - .planning/phases/02-contact-discovery/02-RESEARCH.md (Assumptions A1–A4 resolution path)
  </read_first>
  <action>
    If Task 1 reported "fields match": make no code change beyond, if helpful, updating the inline comment near each `.get()` lookup to note "CONFIRMED live 2026-07 — field name verified" (mirroring the confirmed-comment convention in apollo/client.py get_credit_balance). If Task 1 reported differing field names: update ONLY the affected `.get("<key>")` lookups in `search_people` (people/organization fields), `bulk_match_people`/`enrich_candidates` (matches/email/email_status/organization_name/credits_consumed), `discovery/logic.py::filter_has_email` (the has_email key), and `db/prospects.py` domain/email derivation to the real names observed live. Keep all defensive `.get()` degradation — do not switch to bracket indexing. Do not alter banner strings, batching, or retry logic. Re-run the full suite.
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` fully green after reconciliation
    - Every field lookup touched remains a `.get(...)` call (grep shows no new bare-bracket Apollo field access)
    - If fields matched, the confirmed-comment is present; if fields differed, the changed keys match the names recorded in Task 1's resume signal
  </acceptance_criteria>
  <done>Field-name lookups are confirmed or corrected against live Apollo, and the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification harness → live Apollo | Real authenticated calls that can spend real credits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure / Resource | live bulk_match during verify spends real credits | mitigate | cap the live bulk_match batch at ≤3 real people; one search (0 credits) + one tiny enrichment only |
| T-02-04 | Information Disclosure | APOLLO_API_KEY handled in the verify shell | mitigate | read the key from st.secrets/secrets.toml; never paste it into the summary or logs |
| T-02-SC | Tampering | pip installs | accept | Phase introduces ZERO new packages (RESEARCH Package Legitimacy Audit: N/A) — no install task exists |
</threat_model>

<verification>
- Live search + live bulk_match executed; real field names recorded
- Any mismatch reconciled in .get() lookups only
- `pytest -q` full suite green
</verification>

<success_criteria>
- Apollo search + bulk_match response field names confirmed against the live account (DISC-02 has_email, DISC-03 email/email_status)
- Code reconciled to real field names where they differed, retaining defensive degradation
- Full test suite green — safe to build the UI on these shapes
</success_criteria>

<output>
Create `.planning/phases/02-contact-discovery/02-03-SUMMARY.md` when done. Record the confirmed/corrected field names explicitly so Plan 04 can rely on them.
</output>
