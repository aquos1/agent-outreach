---
phase: 01-foundation
plan: 02
type: tdd
wave: 1
depends_on: [01-01]
files_modified:
  - db/schema.py
  - tests/test_schema.py
autonomous: true
requirements: [DEDUP-01, SC-3, SC-4]
must_haves:
  truths:
    - "Running ensure_schema() on a fresh path creates the prospect, email_events, and free_email_domains tables plus the contacted_registry view with no manual setup"
    - "Running ensure_schema() a second time against the same file does not error (idempotent across restarts)"
    - "Rows written before a fresh sqlite3.connect() to the same file remain queryable (registry persists across restarts)"
    - "The contacted_registry view nulls dedupable_domain for seeded free-email domains and passes company domains through"
  artifacts:
    - path: "db/schema.py"
      provides: "Idempotent SQLite schema bootstrap for the dedup registry (DEDUP-01)"
      exports: ["ensure_schema"]
      min_lines: 40
  key_links:
    - from: "db/schema.py"
      to: "free_email_domains table"
      via: "INSERT OR IGNORE seed"
      pattern: "free_email_domains"
    - from: "contacted_registry view"
      to: "free_email_domains table"
      via: "subquery excluding free domains from dedupable_domain"
      pattern: "dedupable_domain"
---

<objective>
Deliver the persistent SQLite data registry — the DEDUP-01 requirement and success criteria 3 (auto-created schema, no manual setup) and 4 (registry persists across restarts). This is the DB read/write leg of the walking skeleton: `ensure_schema()` is idempotent, seeds the free-email-domain exclusion list, and exposes the `contacted_registry` view that Phase 2's DEDUP-02 dedup query reads directly.

Purpose: Every later phase writes prospect/email_events rows into this schema; getting the shape and the dedup semantics right now is DEDUP-01's mandate.
Output: `db/schema.py` with a tested, idempotent `ensure_schema()`.
</objective>

<execution_context>
@/Users/yashpersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yashpersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Contract this plan implements (from 01-PATTERNS.md / 01-RESEARCH.md Code Examples). -->
db/schema.py:
  ensure_schema(db_path: str = "db/outreach.db") -> None

Tables:
  prospect(id PK, name NOT NULL, company, company_domain, apollo_person_id,
           apollo_contact_id, email, path, status CHECK IN
           ('found','selected','enriched','contact_created','drafted','approved','sequenced')
           DEFAULT 'found', sequence_id, created_at, updated_at)
  email_events(id PK, prospect_id FK->prospect(id), apollo_contact_id, event_type NOT NULL,
               occurred_at, raw_payload, polled_at)
  free_email_domains(domain PK)  seeded: gmail.com, outlook.com, yahoo.com, hotmail.com, icloud.com

View:
  contacted_registry -> exposes id, apollo_person_id, apollo_contact_id, company_domain,
     dedupable_domain (NULL when company_domain is NULL or a free-email domain, else company_domain),
     status; WHERE apollo_contact_id IS NOT NULL
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Idempotent tables and free-email-domain seed</name>
  <files>db/schema.py, tests/test_schema.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Code Examples: Idempotent Schema Bootstrap, lines 286-356; Pitfall 3 lines 268-274; Pitfall 4 lines 275-280)
    - .planning/phases/01-foundation/01-PATTERNS.md (db/schema.py Pattern Assignment lines 72-146)
    - tests/test_schema.py (the RED tests created in 01-01 — implement to make them GREEN; add assertions if a named test lacks a body)
    - tests/conftest.py (the tmp_db_path fixture to use)
  </read_first>
  <behavior>
    - test_ensure_schema_idempotent: calling ensure_schema(path) twice against the same file completes without raising
    - test_persistence_across_reconnect: after ensure_schema + INSERT of a prospect row, a NEW sqlite3.connect() to the same file can SELECT that row back (registry survives a simulated restart) — SC-4
    - Seeded free_email_domains contains exactly the 5 rows gmail.com, outlook.com, yahoo.com, hotmail.com, icloud.com and re-running does not duplicate them (INSERT OR IGNORE)
  </behavior>
  <action>
    Implement `ensure_schema(db_path: str = "db/outreach.db") -> None` in db/schema.py. Define a module-level `DDL` string using `CREATE TABLE IF NOT EXISTS` for `prospect`, `email_events`, and `free_email_domains` exactly per the interfaces block above (prospect.status uses the 7-value CHECK constraint and DEFAULT 'found'; email_events.prospect_id REFERENCES prospect(id)). Seed `free_email_domains` with `INSERT OR IGNORE INTO free_email_domains (domain) VALUES ('gmail.com'),('outlook.com'),('yahoo.com'),('hotmail.com'),('icloud.com')`. Open with `sqlite3.connect(db_path)`, run `conn.executescript(DDL)`, `conn.commit()`, and close in a `finally`. Do NOT f-string any value into the DDL and do NOT hardcode the free-domain list anywhere except this seed table (Pitfall 4). Ensure the parent directory of db_path exists before connecting (create it if missing) so a fresh checkout auto-creates the DB with no manual step (SC-3). Flesh out test_ensure_schema_idempotent and test_persistence_across_reconnect in tests/test_schema.py to assert the behavior above.
  </action>
  <verify>
    <automated>python -m pytest tests/test_schema.py::test_ensure_schema_idempotent tests/test_schema.py::test_persistence_across_reconnect -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_schema.py::test_ensure_schema_idempotent -x -q` passes
    - `python -m pytest tests/test_schema.py::test_persistence_across_reconnect -x -q` passes
    - `grep -v '^#' db/schema.py | grep -c "CREATE TABLE IF NOT EXISTS"` returns 3
    - `db/schema.py` seeds exactly five free-email domains via `INSERT OR IGNORE` and contains no other literal list of those domains
    - No f-string or `%`/`+` string interpolation of a variable into any SQL statement in db/schema.py (DDL is a static constant)
  </acceptance_criteria>
  <done>ensure_schema() creates all three tables idempotently, seeds the free-domain table, and written rows survive a fresh connection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: contacted_registry view with dedup semantics</name>
  <files>db/schema.py, tests/test_schema.py</files>
  <read_first>
    - db/schema.py (current state from Task 1 — extend it)
    - .planning/phases/01-foundation/01-RESEARCH.md (VIEW_DDL in Code Examples lines 329-346; Pitfall 3 view drop/recreate lines 268-274; CONTEXT.md dedup scope decision echoed at lines 41, 275-280)
    - tests/test_schema.py (test_registry_excludes_free_domains RED test)
  </read_first>
  <behavior>
    - test_registry_excludes_free_domains: a prospect row with company_domain 'acme.com' and a non-null apollo_contact_id appears in contacted_registry with dedupable_domain = 'acme.com'; a row with company_domain 'gmail.com' appears with dedupable_domain = NULL
    - A prospect row with apollo_contact_id IS NULL does NOT appear in contacted_registry (only contact_created-or-later rows)
    - ensure_schema() run twice does not raise "view already exists"
  </behavior>
  <action>
    Add a module-level `VIEW_DDL` string to db/schema.py and run it via a second `conn.executescript(VIEW_DDL)` inside ensure_schema() (before commit). VIEW_DDL must pair `DROP VIEW IF EXISTS contacted_registry;` immediately before `CREATE VIEW contacted_registry AS ...` (Pitfall 3 — views have no reliable IF NOT EXISTS guard). The view SELECTs id, apollo_person_id, apollo_contact_id, company_domain, a CASE expression aliased `dedupable_domain` (NULL when company_domain IS NULL, NULL when company_domain IN (SELECT domain FROM free_email_domains), else company_domain), and status FROM prospect WHERE apollo_contact_id IS NOT NULL. Contact-ID-level dedup applies to all rows regardless of domain; domain-level dedup only applies to `dedupable_domain` (company-owned) domains — this encodes the CONTEXT.md discretion decision for DEDUP-01. Implement test_registry_excludes_free_domains to assert the acme.com vs gmail.com behavior above.
  </action>
  <verify>
    <automated>python -m pytest tests/test_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_schema.py::test_registry_excludes_free_domains -x -q` passes
    - `python -m pytest tests/test_schema.py -x -q` passes all three schema tests
    - `db/schema.py` contains `DROP VIEW IF EXISTS contacted_registry` on a line preceding `CREATE VIEW contacted_registry`
    - Calling `ensure_schema()` twice in one process against the same temp file does not raise `sqlite3.OperationalError`
    - The view yields `dedupable_domain = NULL` for a `gmail.com` company_domain and the literal domain for `acme.com`
  </acceptance_criteria>
  <done>contacted_registry view exists, drops/recreates idempotently, and correctly nulls dedupable_domain for free-email domains — DEDUP-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application code → SQLite | Values written into the prospect/email_events tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | SQL statements against `prospect`/`email_events` | mitigate | Establish the parameterized-query pattern now: `db/schema.py` runs only static DDL (no variable interpolation); acceptance criteria assert no f-string/`%`/`+` interpolation into SQL. Phase 2+ row writes must use `?` placeholders (RESEARCH.md Security Domain line 591). |
| T-02-02 | Information Disclosure | `db/outreach.db` file contents | accept | DB holds internal outreach data only; file is gitignored (T-01-02). No end-user-facing exposure path in this single-tenant internal tool (RESEARCH.md V4 Access Control: N/A). |
</threat_model>

<verification>
- `python -m pytest tests/test_schema.py -v` all green
- `ensure_schema()` on a brand-new path auto-creates the parent dir and DB (SC-3)
- Rows persist across a fresh `sqlite3.connect()` (SC-4)
</verification>

<success_criteria>
DEDUP-01 delivered: prospect + email_events + free_email_domains tables and the contacted_registry dedup view are created idempotently on first launch with no manual setup, and the registry persists across restarts.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done.
</output>
