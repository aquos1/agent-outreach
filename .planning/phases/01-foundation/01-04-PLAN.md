---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-02, 01-03]
files_modified:
  - app.py
  - pages/health_page.py
autonomous: false
requirements: [SC-1, SC-2, SC-3]
user_setup:
  - service: apollo
    why: "The app requires a valid Master API key and sending domain in secrets to render green"
    env_vars:
      - name: APOLLO_API_KEY
        source: "Apollo Settings → Integrations → API Keys (Master)"
      - name: SENDING_DOMAIN
        source: "Your club's outreach sending domain"
    dashboard_config:
      - task: "Copy .streamlit/secrets.toml.example to .streamlit/secrets.toml and fill in real values"
        location: "Local project .streamlit/ directory (gitignored)"
must_haves:
  truths:
    - "Running `streamlit run app.py` opens a System Health page as the default landing screen (D-04)"
    - "The page shows three indicators: Apollo API key validity (pass/fail), Apollo credits remaining (informational metric), and mailbox deliverability SPF/DMARC/DKIM (pass/fail/unknown)"
    - "A missing or invalid Apollo key renders a plain-language error banner and hard-blocks the rest of the app via st.stop() (D-01), never a stack trace (SC-2)"
    - "The SQLite database is auto-created at boot with no manual setup (SC-3)"
    - "A low/zero credit balance is shown but never blocks (D-03); an SPF/DMARC/DKIM failure renders but does not st.stop() (D-02)"
  artifacts:
    - path: "app.py"
      provides: "Entrypoint: schema bootstrap at boot, st.navigation gate keyed on Apollo health"
      exports: []
      min_lines: 20
    - path: "pages/health_page.py"
      provides: "System Health landing page rendering all three checks per UI-SPEC"
      min_lines: 50
  key_links:
    - from: "app.py"
      to: "db.schema.ensure_schema"
      via: "boot-time call before rendering"
      pattern: "ensure_schema\\("
    - from: "app.py"
      to: "apollo.client.check_apollo_health"
      via: "D-01 gate driving st.stop()"
      pattern: "check_apollo_health"
    - from: "pages/health_page.py"
      to: "mailbox.dns_checks"
      via: "SPF/DMARC/DKIM status blocks"
      pattern: "check_spf|check_dmarc|check_dkim"
---

<objective>
Wire the walking skeleton end-to-end: `app.py` boots, auto-creates the SQLite schema, runs the Apollo health gate, and routes to a `System Health` landing page that renders all three checks per the locked UI-SPEC. This is the slice that makes the app a real, runnable application — after this plan a teammate can `streamlit run app.py` and see live pass/fail status (SC-1), get a clear error banner on a bad key (SC-2), and have the DB auto-created (SC-3), with D-01/D-02/D-03 gating enforced.

Purpose: Turns the tested modules from Plans 01-02 and 01-03 into the user-facing health page — the phase's only UI surface.
Output: `app.py`, `pages/health_page.py`, and a verified running app.
</objective>

<execution_context>
@/Users/yashpersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yashpersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- Contracts consumed here, implemented in Plans 01-02 and 01-03. -->
db/schema.py:        ensure_schema(db_path: str = "db/outreach.db") -> None
apollo/client.py:    check_apollo_health(api_key: str) -> tuple[bool, str]
                     get_credit_balance(api_key: str) -> tuple[int | None, str]
mailbox/dns_checks.py: check_spf(domain) -> tuple[bool, str]
                       check_dmarc(domain) -> tuple[bool, str]
                       check_dkim(domain) -> tuple[str, str]  # ('pass'|'unknown', msg)

Secrets (via st.secrets):  APOLLO_API_KEY, SENDING_DOMAIN

UI-SPEC bindings (01-UI-SPEC.md — authoritative):
  Title: st.title("System Health"); subtitle caption "Checked automatically every time the app loads."
  Apollo key: st.status/st.error, hard block -> st.stop() on fail (D-01), icon :material/check_circle: / :material/cancel:
  Apollo credits: st.metric(label="Apollo Credits Remaining", value=N), never blocks (D-03); if None show muted caption
  SPF/DMARC fail: st.warning (amber, not red), icon :material/error:
  DKIM unknown: st.warning + st.checkbox("I've configured DKIM for this domain"), icon :material/help:
  CTA: st.button("Recheck Connection", icon=":material/refresh:") below the Apollo card
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: app.py — boot schema, secrets gate, Apollo-gated navigation</name>
  <files>app.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1 gated navigation lines 181-202; Secrets Access Pattern lines 471-481; Architecture diagram lines 117-149)
    - .planning/phases/01-foundation/01-PATTERNS.md (app.py Pattern Assignment lines 36-68; Gating semantics lines 327-333)
    - db/schema.py, apollo/client.py (the functions app.py calls — read their signatures/returns)
  </read_first>
  <action>
    Create app.py as the Streamlit entrypoint. At the top of every run: (1) call `ensure_schema()` (default db path) so the SQLite DB auto-creates on first launch with no manual setup — SC-3; (2) read `apollo_key = st.secrets.get("APOLLO_API_KEY")` and `sending_domain = st.secrets.get("SENDING_DOMAIN")`; if `apollo_key` is falsy, render `st.error("APOLLO_API_KEY is not set. Add it to .streamlit/secrets.toml (local) or the Community Cloud secrets console (production).")` and `st.stop()`. Register navigation with `st.navigation` using a single default page `st.Page("pages/health_page.py", title="System Health", default=True)` (Phase 2+ will append pages here — leave a commented extension point). Do NOT put the per-check rendering in app.py; that lives in pages/health_page.py. The Apollo hard-block itself is rendered inside health_page.py (so the credit/mailbox blocks below it can render when Apollo passes) — app.py's responsibility is schema boot + secrets presence gate + navigation. Never log or print the api_key.
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('app.py').read()); print('AST_OK')" && grep -q "ensure_schema" app.py && grep -q "st.navigation" app.py && grep -q "st.secrets" app.py && grep -q "health_page.py" app.py</automated>
  </verify>
  <acceptance_criteria>
    - `app.py` parses as valid Python (ast.parse succeeds)
    - `app.py` calls `ensure_schema(` before rendering the page (SC-3)
    - `app.py` reads `st.secrets` for `APOLLO_API_KEY` and calls `st.stop()` when it is missing, after an `st.error` banner (SC-2)
    - `app.py` registers `st.navigation` with `pages/health_page.py` as the default page (D-04)
    - No `print(` of, or logging of, `api_key`/`APOLLO_API_KEY` value in app.py
  </acceptance_criteria>
  <done>app.py bootstraps the schema, gates on secret presence with a clear banner, and routes to the health page as the default landing screen.</done>
</task>

<task type="auto">
  <name>Task 2: pages/health_page.py — render three checks with D-01/D-02/D-03 gating</name>
  <files>pages/health_page.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (ENTIRE file — Health Check State System table lines 106-125, Copywriting Contract lines 127-146, icon assignments, color/typography roles — this is the authoritative rendering contract)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2 st.status blocks lines 204-226)
    - .planning/phases/01-foundation/01-PATTERNS.md (health_page.py Pattern Assignment lines 265-286)
    - apollo/client.py, mailbox/dns_checks.py (functions this page calls)
  </read_first>
  <action>
    Create pages/health_page.py rendering the System Health page per 01-UI-SPEC.md. Render `st.title("System Health")` and `st.caption("Checked automatically every time the app loads.")`. Read `apollo_key`/`sending_domain` from `st.secrets`. APOLLO CONNECTION card (`st.header("Apollo Connection")`): wrap `check_apollo_health(apollo_key)` in an `st.status`; on pass update to state="complete" with `:material/check_circle:`; on fail render the returned banner via `st.error`, an "App blocked" caption ("The rest of the app is unavailable until this is fixed. Fix the issue above, then click Recheck Connection."), set status state="error", then `st.stop()` (D-01 hard block — nothing below renders). Add `st.button("Recheck Connection", icon=":material/refresh:")` below this card (a click reruns the script, re-running all checks). APOLLO CREDITS card (`st.header("Apollo Credits")`): call `get_credit_balance(apollo_key)`; if a number, `st.metric(label="Apollo Credits Remaining", value=<n>)` (neutral, no delta color); if None, render the muted caption "Credit balance unavailable — this won't stop you from using the app." NEVER st.stop() here (D-03). MAILBOX DELIVERABILITY card (`st.header("Mailbox Deliverability")`) with framing caption per UI-SPEC: run `check_spf`, `check_dmarc` against `sending_domain` — render green success on True, amber `st.warning` with the exact SPF/DMARC fail copy and `:material/error:` on False; run `check_dkim` — render green on 'pass', amber `st.warning` with the DKIM-unknown copy, `:material/help:`, AND `st.checkbox("I've configured DKIM for this domain")` on 'unknown'. NEVER st.stop() for any mailbox result (D-02 soft block — no send/enroll actions exist yet in Phase 1). Wrap every check call so an unexpected exception renders an `st.error` banner rather than Streamlit's traceback (SC-2). Use only the exact copy strings and Material icons from 01-UI-SPEC.md.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('pages/health_page.py').read()); print('AST_OK')" && grep -q "System Health" pages/health_page.py && grep -q "check_apollo_health" pages/health_page.py && grep -q "st.metric" pages/health_page.py && grep -q "check_spf" pages/health_page.py && grep -q "check_dkim" pages/health_page.py && grep -q "Recheck Connection" pages/health_page.py</automated>
  </verify>
  <acceptance_criteria>
    - `pages/health_page.py` parses as valid Python
    - Page renders `st.title("System Health")` and the subtitle caption
    - Apollo failure path renders `st.error` + calls `st.stop()`; the credit and mailbox sections appear only after the Apollo pass path (D-01)
    - Credit balance uses `st.metric(label="Apollo Credits Remaining", ...)` and has NO `st.stop()` in the credits section (D-03)
    - SPF/DMARC failures use `st.warning` (not `st.error`); DKIM 'unknown' renders `st.warning` plus `st.checkbox("I've configured DKIM for this domain")` (D-02); no `st.stop()` anywhere in the mailbox section
    - The exact locked copy strings from 01-UI-SPEC.md are present for the 401/403 banners, SPF fail, DMARC fail, DKIM unknown, and credit-unavailable messages
    - `st.button("Recheck Connection", icon=":material/refresh:")` is present
  </acceptance_criteria>
  <done>The health page renders all three indicators with the exact UI-SPEC copy/icons and enforces D-01 hard block, D-02 soft (render-only), and D-03 informational gating.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — boot the app and confirm health page + error banner</name>
  <what-built>
    A runnable Streamlit app whose default landing screen is the System Health page, showing Apollo key validity (pass/fail), Apollo credits (informational metric), and mailbox SPF/DMARC/DKIM (pass/fail/unknown), with D-01 hard block on a bad Apollo key. The SQLite DB auto-creates at boot.
  </what-built>
  <how-to-verify>
    1. Ensure `.streamlit/secrets.toml` exists locally (copy from `.streamlit/secrets.toml.example`) with a VALID `APOLLO_API_KEY` and a real `SENDING_DOMAIN`.
    2. Run `streamlit run app.py`. Confirm the browser opens to a page titled "System Health" (default landing — D-04).
    3. Confirm three sections render: "Apollo Connection" (green/pass), "Apollo Credits" (a number metric, or the muted "unavailable" caption — either is acceptable per D-03), and "Mailbox Deliverability" (SPF/DMARC pass or amber warning; DKIM pass or amber "Unknown" with the self-attestation checkbox).
    4. Confirm `db/outreach.db` was created automatically (SC-3) — check the file exists after boot.
    5. Now set `APOLLO_API_KEY` to an invalid value (or remove it) in secrets and reload. Confirm a plain-language error banner appears (NOT a Python stack trace — SC-2) and that no other section renders below it (D-01 hard block).
    6. Restore the valid key, reload, and confirm the page returns to green.
    7. WAVE-0 CREDIT FIELD: if the credits metric shows "unavailable" with a valid key, capture the raw `usage_stats/api_usage_stats` JSON once (a one-off authenticated call) and confirm/adjust the credit field name in `apollo/client.py` per RESEARCH.md Open Q #1 — then re-run.
  </how-to-verify>
  <resume-signal>Type "approved" if all three indicators render correctly and the bad-key path shows a banner (not a traceback); otherwise describe what rendered incorrectly.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| st.secrets → app | API key and sending domain read at boot (server-side only) |
| exceptions → user | Traceback must never reach a non-technical user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Streamlit traceback view on an Apollo/DNS exception | mitigate | Every check call in health_page.py wrapped in try/except → `st.error` banner; app.py secrets gate renders a banner + `st.stop()` instead of raising (SC-2, RESEARCH.md line 590). |
| T-04-02 | Information Disclosure | APOLLO_API_KEY read from st.secrets | mitigate | Read server-side via `st.secrets` only; never rendered to the page, printed, or logged. Streamlit has no client-side code path (RESEARCH.md line 589). |
| T-04-03 | Elevation of Privilege | Reaching downstream app functionality with an invalid Apollo key | mitigate | D-01 hard block: `st.stop()` after the Apollo failure banner halts the script run so no other section/page renders until the key is valid. |
</threat_model>

<verification>
- `streamlit run app.py` opens the System Health page as default (D-04)
- Valid key → three indicators render (SC-1); `db/outreach.db` auto-created (SC-3)
- Invalid/missing key → plain-language banner, no traceback, hard block (SC-2, D-01)
- Credit low/zero never blocks (D-03); SPF/DMARC/DKIM issues render but never st.stop() (D-02)
</verification>

<success_criteria>
Phase success criteria 1, 2, and 3 verified end-to-end: the health page displays Apollo key validity, credit balance, and mailbox deliverability as pass/fail; a bad key produces a clear error message (not a stack trace); and the SQLite DB is auto-created on first launch. Combined with Plan 01-02 (criterion 4 persistence), the walking skeleton is complete.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done.
</output>
