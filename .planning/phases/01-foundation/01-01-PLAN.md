---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements.txt
  - requirements-dev.txt
  - pyproject.toml
  - .gitignore
  - .streamlit/config.toml
  - .streamlit/secrets.toml.example
  - db/__init__.py
  - apollo/__init__.py
  - mailbox/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_schema.py
  - tests/test_apollo_client.py
  - tests/test_dns_checks.py
autonomous: true
requirements: [DEDUP-01, SC-1, SC-2]
must_haves:
  truths:
    - "A developer can run pytest and see the full test suite collect without collection errors"
    - "The three RED test files exist and fail (source modules not yet implemented) rather than being absent"
    - "Streamlit theme, secrets template, and gitignore exist so no real secret or .db file can be committed"
  artifacts:
    - path: "requirements.txt"
      provides: "Pinned runtime dependencies (streamlit, requests, dnspython)"
      contains: "streamlit=="
    - path: "requirements-dev.txt"
      provides: "Pinned test dependency (pytest)"
      contains: "pytest"
    - path: "pyproject.toml"
      provides: "pytest testpaths config"
      contains: "testpaths"
    - path: ".gitignore"
      provides: "Excludes secrets and db files from git"
      contains: ".streamlit/secrets.toml"
    - path: ".streamlit/config.toml"
      provides: "Locked theme tokens from UI-SPEC"
      contains: "primaryColor"
    - path: "tests/conftest.py"
      provides: "Temp SQLite DB fixture + requests/dns mock helpers"
      min_lines: 20
    - path: "tests/test_schema.py"
      provides: "RED tests for DEDUP-01 schema behavior"
      contains: "test_ensure_schema_idempotent"
    - path: "tests/test_apollo_client.py"
      provides: "RED tests for Apollo health/credit checks"
      contains: "test_check_apollo_health"
    - path: "tests/test_dns_checks.py"
      provides: "RED tests for SPF/DMARC/DKIM checks"
      contains: "test_check_spf_dmarc_dkim"
  key_links:
    - from: "pyproject.toml"
      to: "tests/"
      via: "testpaths setting"
      pattern: "testpaths"
    - from: ".gitignore"
      to: ".streamlit/secrets.toml"
      via: "ignore rule"
      pattern: "secrets\\.toml"
---

<objective>
Establish the project scaffold and the RED test harness for the Foundation walking skeleton. This is Wave 0 — it installs everything the downstream slices verify against: pinned dependencies, pytest configuration, shared test fixtures, the locked Streamlit theme, the secrets template, and the failing test files that Waves 1-2 will turn green.

Purpose: Every downstream plan's `<verify>` runs against pytest and the fixtures created here. Per 01-VALIDATION.md, Wave 0 must create the test scaffolds before any source exists.
Output: A runnable (but RED) test suite, dependency manifests, gitignore, and the `.streamlit/config.toml` theme contract.
</objective>

<execution_context>
@/Users/yashpersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yashpersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-PATTERNS.md

<interfaces>
<!-- Function signatures the RED tests import (implemented in later waves). Tests must import these INSIDE test bodies so collection succeeds before source exists. -->

db/schema.py (Plan 01-02):
  ensure_schema(db_path: str = "db/outreach.db") -> None

apollo/client.py (Plan 01-03):
  check_apollo_health(api_key: str) -> tuple[bool, str]
  get_credit_balance(api_key: str) -> tuple[int | None, str]

mailbox/dns_checks.py (Plan 01-03):
  check_spf(domain: str) -> tuple[bool, str]
  check_dmarc(domain: str) -> tuple[bool, str]
  check_dkim(domain: str) -> tuple[str, str]   # ('pass' | 'unknown', message)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffold, dependency pins, gitignore, and theme</name>
  <files>requirements.txt, requirements-dev.txt, pyproject.toml, .gitignore, .streamlit/config.toml, .streamlit/secrets.toml.example, db/__init__.py, apollo/__init__.py, mailbox/__init__.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack table for exact version pins; Recommended Project Structure for directory layout; Environment Availability for Python 3.13 note)
    - .planning/phases/01-foundation/01-UI-SPEC.md (the `.streamlit/config.toml` theme block, lines 27-40)
    - CLAUDE.md (stack constraints, secrets convention)
  </read_first>
  <action>
    Create the project scaffold with these exact pinned versions in requirements.txt: `streamlit==1.59.2`, `requests==2.34.2`, `dnspython==2.8.0` (do NOT include checkdmarc — RESEARCH.md defers it). Create requirements-dev.txt pinning `pytest==9.0.2`. Create pyproject.toml with a `[tool.pytest.ini_options]` section setting `testpaths = ["tests"]` and `addopts = "-q"`. Create .gitignore containing at minimum `.streamlit/secrets.toml`, `*.db`, `db/outreach.db`, `__pycache__/`, `.pytest_cache/`, and `*.pyc`. Create .streamlit/config.toml with the exact `[theme]` block from 01-UI-SPEC.md: primaryColor `#2563EB`, backgroundColor `#FFFFFF`, secondaryBackgroundColor `#F0F2F6`, textColor `#0F172A`, font `sans serif`, baseFontSize `16`, headingFontWeights `600`. Create .streamlit/secrets.toml.example (a committed template, NOT the real secrets file) with placeholder keys `APOLLO_API_KEY = "your-master-api-key"` and `SENDING_DOMAIN = "outreach.yourclub.org"`. Create empty package markers db/__init__.py, apollo/__init__.py, mailbox/__init__.py. Do NOT create .streamlit/secrets.toml (real secrets) — it is developer-supplied and gitignored.
  </action>
  <verify>
    <automated>test -f requirements.txt && test -f requirements-dev.txt && test -f pyproject.toml && test -f .gitignore && test -f .streamlit/config.toml && test -f .streamlit/secrets.toml.example && grep -q 'streamlit==1.59.2' requirements.txt && grep -q 'testpaths' pyproject.toml && grep -q 'secrets.toml' .gitignore && grep -q 'primaryColor' .streamlit/config.toml && ! test -f .streamlit/secrets.toml</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` contains the three lines `streamlit==1.59.2`, `requests==2.34.2`, `dnspython==2.8.0` and does NOT contain `checkdmarc`
    - `requirements-dev.txt` contains `pytest==9.0.2`
    - `pyproject.toml` contains a `[tool.pytest.ini_options]` block with `testpaths = ["tests"]`
    - `.gitignore` contains `.streamlit/secrets.toml` and `*.db`
    - `.streamlit/config.toml` `[theme]` block matches 01-UI-SPEC.md exactly (primaryColor `#2563EB`, headingFontWeights `600`, baseFontSize `16`)
    - `.streamlit/secrets.toml.example` exists with placeholder (non-real) values; `.streamlit/secrets.toml` does NOT exist in the repo
    - `db/__init__.py`, `apollo/__init__.py`, `mailbox/__init__.py` all exist
  </acceptance_criteria>
  <done>Scaffold, dependency pins, gitignore, theme, and secrets template all exist; no real secret file and no .db file are tracked by git.</done>
</task>

<task type="auto">
  <name>Task 2: Test infrastructure and RED test files</name>
  <files>tests/__init__.py, tests/conftest.py, tests/test_schema.py, tests/test_apollo_client.py, tests/test_dns_checks.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (Per-Task Verification Map — the exact test names and behaviors required)
    - .planning/phases/01-foundation/01-RESEARCH.md (Code Examples for schema, apollo client, dns checks — the behavior these tests assert; Validation Architecture section lines 539-571)
    - .planning/phases/01-foundation/01-PATTERNS.md (test-to-requirement map, conftest fixture guidance lines 290-304)
  </read_first>
  <action>
    Create tests/__init__.py (empty). Create tests/conftest.py providing: (a) a `tmp_db_path` fixture returning a per-test temp SQLite file path via pytest's `tmp_path`, cleaned up automatically; (b) a `mock_requests_response` helper/fixture that builds a fake object with `.status_code` and `.json()` for mocking `requests.get`/`requests.post`; (c) a `mock_dns_txt` helper/fixture for monkeypatching `dns.resolver.resolve` to return canned TXT answers or raise `NXDOMAIN`/`NoAnswer`. Create the three RED test files implementing exactly the tests named in 01-VALIDATION.md: `test_schema.py` with `test_ensure_schema_idempotent`, `test_registry_excludes_free_domains`, `test_persistence_across_reconnect`; `test_apollo_client.py` with `test_check_apollo_health` (asserts `(False, ...)` on 401 and 403, `(True, ...)` on 200 with `is_logged_in: true`); `test_dns_checks.py` with `test_check_spf_dmarc_dkim` (asserts SPF/DMARC True on a `v=spf1`/`v=DMARC1` record and False when absent, and DKIM returns `'unknown'` when no common selector resolves, never a hard fail). CRITICAL: import the target functions (`from db.schema import ensure_schema`, etc.) INSIDE each test body, not at module top level, so `pytest --collect-only` succeeds before source modules exist. These tests are expected to FAIL now (RED) because the source modules do not yet exist — that is correct.
  </action>
  <verify>
    <automated>python -m pytest tests/ --collect-only -q && echo "COLLECT_OK"; python -m pytest tests/ -q; test $? -ne 0 && echo "RED_AS_EXPECTED"</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ --collect-only -q` exits 0 and lists `test_ensure_schema_idempotent`, `test_registry_excludes_free_domains`, `test_persistence_across_reconnect`, `test_check_apollo_health`, `test_check_spf_dmarc_dkim`
    - `python -m pytest tests/ -q` exits non-zero (tests are RED — source not yet implemented)
    - `tests/conftest.py` defines a temp-DB fixture and mock helpers for `requests` and `dns.resolver` (min 20 lines)
    - Target-module imports occur inside test bodies, not at module top (grep: `grep -c 'from db.schema import\|from apollo.client import\|from mailbox.dns_checks import' tests/test_*.py` returns matches only within `def test_` scope — no top-level import line before the first `def`)
  </acceptance_criteria>
  <done>Test suite collects cleanly and runs RED; conftest fixtures exist for temp DB and network mocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → git | Secret files and DB files must never cross into version control |
| PyPI → local env | Third-party packages installed into the runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.streamlit/secrets.toml` (APOLLO_API_KEY, SENDING_DOMAIN) | mitigate | `.gitignore` excludes `.streamlit/secrets.toml`; only `.streamlit/secrets.toml.example` with placeholder values is committed. Task 1 verify asserts real secrets file is absent. |
| T-01-02 | Information Disclosure | SQLite `*.db` files | mitigate | `.gitignore` excludes `*.db` and `db/outreach.db` so contacted-registry data never lands in git |
| T-01-SC | Tampering | pip installs (streamlit, requests, dnspython, pytest) | mitigate | RESEARCH.md `## Package Legitimacy Audit` scanned all 4 packages `[OK]` via slopcheck on 2026-07-20; names match CLAUDE.md's locked stack (`[VERIFIED: PyPI registry]`). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages — no blocking human checkpoint required. checkdmarc is deferred and NOT installed this phase. |
</threat_model>

<verification>
- `python -m pytest tests/ --collect-only -q` exits 0
- `python -m pytest tests/ -q` exits non-zero (RED, expected until Waves 1-2)
- `git status --porcelain` shows no `.streamlit/secrets.toml` and no `*.db` staged
</verification>

<success_criteria>
Wave 0 test infrastructure and scaffold complete: dependencies pinned, pytest configured, RED tests collectable and failing, theme + gitignore + secrets template in place. Enables all downstream `<verify>` commands.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done.
</output>
