---
phase: 04-review-queue-and-sequence-enrollment
plan: 04
type: execute
wave: 4
depends_on: ["04-02", "04-03", "04-06"]
files_modified:
  - pages/review_queue_page.py
  - .streamlit/secrets.toml.example
autonomous: true
requirements: [QUEUE-03, QUEUE-04]

must_haves:
  truths:
    - "QUEUE-03: every queued row has a checkbox that starts checked, and unchecking one excludes that contact from the next approve action"
    - "D-09: both 'Approve Selected' and 'Approve All' open a confirmation dialog naming the contact count and the path before any Apollo call fires; dismissing it changes nothing"
    - "QUEUE-04: confirming creates Apollo contacts with run_dedupe and enrolls the resulting contact IDs into the path's pre-configured sequence"
    - "D-07/D-12: after the call, an Apollo-confirmed row shows a green Enrolled badge in place of its checkbox, and a skipped row keeps its checkbox and shows an orange badge carrying Apollo's stated reason"
    - "D-14/D-16: every approved contact's AI opening line is sent to Apollo as a custom field, so the sequence email can merge it — the opening line is no longer preview-only"
    - "D-17: a contact Apollo returns in existing_contacts gets its opening line applied by a follow-up update, and a failure to apply it is surfaced rather than silently swallowed"
    - "D-08: a skipped or unchecked contact is still at status='drafted' and still appears in the queue on the next page load"
    - "A missing sequence-ID or sending-mailbox secret soft-fails with a plain-language banner on this page only — the rest of the app still boots"
    - "A hard Apollo failure (network/401/403/429/422) changes no prospect rows and leaves the whole batch retryable"
  artifacts:
    - path: "pages/review_queue_page.py"
      provides: "per-row checkboxes, Approve Selected/All, confirmation dialog, approval handler, per-contact outcome badges"
      contains: "st.dialog"
      min_lines: 200
    - path: ".streamlit/secrets.toml.example"
      provides: "the four new Phase 4 secret keys"
      contains: "APOLLO_SENDING_EMAIL_ACCOUNT_ID"
  key_links:
    - from: "pages/review_queue_page.py"
      to: "apollo.client.ensure_custom_field"
      via: "lazy once-per-session resolution of the AI Opening Line field id, cached in session state"
      pattern: "ensure_custom_field\\("
    - from: "pages/review_queue_page.py"
      to: "apollo.client.update_contact_custom_field"
      via: "D-17 follow-up applying the opening line to contacts Apollo already had"
      pattern: "update_contact_custom_field\\("
    - from: "pages/review_queue_page.py"
      to: "apollo.client.create_contacts_bulk"
      via: "contact creation inside the confirmed approval handler"
      pattern: "create_contacts_bulk\\("
    - from: "pages/review_queue_page.py"
      to: "apollo.client.add_contacts_to_sequence"
      via: "sequence enrollment with the path's secret sequence id"
      pattern: "add_contacts_to_sequence\\("
    - from: "pages/review_queue_page.py"
      to: "review.logic.split_enrollment_outcome"
      via: "response-body-driven Enrolled/Skipped determination"
      pattern: "split_enrollment_outcome\\("
    - from: "pages/review_queue_page.py"
      to: "db.prospects.mark_sequenced"
      via: "status advance for Apollo-confirmed prospects only"
      pattern: "mark_sequenced\\("
    - from: "pages/review_queue_page.py"
      to: "st.session_state.approve_result"
      via: "per-contact outcome survives the post-dialog rerun"
      pattern: "st\\.session_state\\.approve_result"
---

<objective>
Complete the phase's headline slice: the teammate selects which drafts to send, confirms once, and watches each contact come back Enrolled or Skipped with Apollo's own reason.

Purpose: This is ROADMAP success criteria 2, 3 and 4 in one interaction — bulk approval, real per-contact confirmation (never a bare HTTP 200), and dedup-registry writes for the contacts that actually got enrolled.
Output: the selection, confirmation and approval handling on `pages/review_queue_page.py`, the custom-field wiring that carries each contact's AI opening line into Apollo (D-14/D-16/D-17), plus the four new secret keys documented in `.streamlit/secrets.toml.example`.
</objective>

## Phase Goal

**As a** non-technical teammate running an outreach campaign, **I want to** review the full queue of drafted emails and approve them in bulk, **so that** the contacts get enrolled into the correct Apollo sequence and the campaign goes live.

This plan delivers "approve them in bulk" and the verified enrollment outcome.

<execution_context>
@/Users/yashpersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yashpersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-CONTEXT.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-RESEARCH.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-PATTERNS.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-UI-SPEC.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-02-SUMMARY.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-03-SUMMARY.md

<interfaces>
<!-- Everything this page consumes already exists after 04-01, 04-02 and 04-03. -->

db/prospects.py:
  get_drafted_by_path(path_slug, db_path="db/outreach.db") -> list[dict]
      # keys: id, name, first_name, last_name, company, company_domain, email, title, path, opening_line
  mark_contact_created(prospect_id: int, apollo_contact_id: str, db_path=...) -> None   # status -> 'contact_created'
  mark_sequenced(prospect_id: int, db_path=...) -> None                                 # status -> 'sequenced'

apollo/client.py:
  create_contacts_bulk(api_key, contacts: list[dict], label_names: list[str],
                       opening_line_field_id: str | None = None) -> tuple[dict | None, str]
      # 200 -> {"created_contacts": [...], "existing_contacts": [...]}; never raises
      # attaches each contact's opening_line as typed_custom_fields when the field id is supplied (D-16)
  ensure_custom_field(api_key, label="AI Opening Line") -> tuple[str | None, str]     # idempotent, D-18
  update_contact_custom_field(api_key, contact_id, field_id, value) -> tuple[bool, str]   # D-17 follow-up
  add_contacts_to_sequence(api_key, sequence_id: str, contact_ids: list[str],
                           send_email_from_email_account_id: str) -> tuple[dict | None, str]
      # 200 -> {"contacts": [...], "skipped_contact_ids": {...}}; never raises; NEVER treat 200 as success

review/logic.py:
  build_contact_payloads(rows) -> list[dict]                             # each entry carries "opening_line"
  map_created_contacts(response, rows) -> dict[int, str]                 # prospect id -> apollo contact id
  split_contacts_by_origin(response, rows) -> tuple[dict[int, str], dict[int, str]]
      # (created_map, existing_map) — the existing_map is exactly the D-17 follow-up set
  split_enrollment_outcome(response, contact_to_prospect) -> tuple[list[int], list[tuple[int, str]]]

db/templates_store.py:
  get_template(path_slug, db_path=...) -> tuple[str, str]
personalization/templates.py:
  assemble_email(path_slug, first_name, company, opening_line, subject_template=None, body_template=None)
discovery/logic.py:
  PATH_OPTIONS, PATH_CONFIG      # label -> {"slug": ...}

Secrets consumed by this plan (D-01, D-02, D-13) — read once at module top via st.secrets.get, never logged:
  APOLLO_SEQUENCE_ID_CLUB_SPONSORSHIP   -> slug "club_sponsorship"
  APOLLO_SEQUENCE_ID_PRODUCTTHON        -> slug "productthon"
  APOLLO_SEQUENCE_ID_CLIENT_SOURCING    -> slug "client_sourcing"
  APOLLO_SENDING_EMAIL_ACCOUNT_ID       -> the one shared sending mailbox for all three paths

Streamlit 1.59.2 verified signatures (04-RESEARCH.md, direct introspection):
  st.dialog(title, *, width='small', dismissible=True, icon=None, on_dismiss='ignore')   # decorator
  st.badge(label, *, icon=None, color='blue', width='content', help=None)
  st.subheader has NO icon kwarg in this version — do not pass one
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Secrets plumbing, per-row selection, and the confirmation dialog gate</name>
  <files>pages/review_queue_page.py, .streamlit/secrets.toml.example</files>
  <read_first>
    - pages/review_queue_page.py (whole file as it stands after 04-02 — the secrets read at module top, the session-state block, the header row and per-row `st.columns` loop whose Select and Status cells are still empty placeholders)
    - .streamlit/secrets.toml.example (the existing three-key file — match its placeholder-string style exactly)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-UI-SPEC.md ("Approve Action & Confirmation Contract", the "Queue Table Contract" checkbox-default row, and every Copywriting Contract row for buttons and the dialog)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-RESEARCH.md (Pattern 3's st.dialog skeleton — the only st.dialog reference point in this project)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-CONTEXT.md (D-01, D-02, D-09, D-13)
    - pages/discovery_page.py (lines 88-140 — the disabled-button and session-state-reset conventions)
  </read_first>
  <action>
In `.streamlit/secrets.toml.example`, append four lines below the existing three, same placeholder-string style, exact key names from D-02 and D-13:
`APOLLO_SEQUENCE_ID_CLUB_SPONSORSHIP = "your-club-sponsorship-sequence-id"`, `APOLLO_SEQUENCE_ID_PRODUCTTHON = "your-productthon-sequence-id"`, `APOLLO_SEQUENCE_ID_CLIENT_SOURCING = "your-client-sourcing-sequence-id"`, `APOLLO_SENDING_EMAIL_ACCOUNT_ID = "your-sending-mailbox-account-id"`. Add a short comment above the block saying these are looked up once in Apollo and hardcoded (D-01: zero live API dependency at enrollment time) and that plan 04-05 resolves the real values.

In `pages/review_queue_page.py`:
1. Define a module-level `SEQUENCE_SECRET_BY_SLUG = {"club_sponsorship": "APOLLO_SEQUENCE_ID_CLUB_SPONSORSHIP", "productthon": "APOLLO_SEQUENCE_ID_PRODUCTTHON", "client_sourcing": "APOLLO_SEQUENCE_ID_CLIENT_SOURCING"}` and read all four secrets once at module top beside the existing `apollo_key` read: `sequence_id = st.secrets.get(SEQUENCE_SECRET_BY_SLUG[slug])` after the slug is known, and `sending_account_id = st.secrets.get("APOLLO_SENDING_EMAIL_ACCOUNT_ID")`. Never log, print, or interpolate these into an error message body. Do NOT add a boot gate in `app.py` — a missing Phase 4 secret must not stop Health or Discovery from working.
2. Soft-fail (this page's never-hard-stop convention): when `sequence_id` or `sending_account_id` is falsy, render `st.warning` naming which secret key is missing and where to add it (".streamlit/secrets.toml locally, or the Community Cloud secrets console"), and render the approve buttons in a disabled state. The queue table and the template editor must still render and work — only approving is blocked.
2b. Resolve the "AI Opening Line" custom field id lazily, once per session (04-RESEARCH.md "Personalization Delivery" §4): `if "opening_line_field_id" not in st.session_state:` call `ensure_custom_field(apollo_key)` and store both the id and its message in session state. Do NOT put this in `app.py`'s boot gate — it would add an Apollo round-trip to Health and Discovery, which do not need the field, and would hard-stop the whole app over a page-scoped capability. Do NOT read it from a secret — D-18 frames this as self-healing check-then-create, and this project's users are explicitly non-technical (they must never have to create a field in Apollo's UI and hand-copy an id). When `ensure_custom_field` returns `None`, render `st.error` with the returned message plus the sentence "Emails would send without the personalized opening line, so approving is paused until this resolves." and disable both approve buttons — the same failure treatment as a missing sequence secret. Wrap the call in `try/except Exception` degrading to that same banner.
3. Fill the per-row Select cell with `st.checkbox("", value=True, key=f"sel_{row['id']}", label_visibility="collapsed")` (QUEUE-03: default checked). Collect the checked prospect ids into a `selected_ids` list as the loop runs. Fill the Status cell with an empty placeholder for now — Task 2 renders badges there.
4. Below the table, render the approve action row with `st.columns(2, gap="small")`: `st.button(f"Approve Selected ({len(selected_ids)})", type="secondary", icon=":material/send:")` in the first column and `st.button(f"Approve All ({len(rows)})", type="primary", icon=":material/send:")` in the second. Live counts in the labels per the Copywriting Contract. Render neither button when the queue is empty (the empty-state branch already returns early). Disable "Approve Selected" when `selected_ids` is empty.
5. Add the confirmation gate (D-09) as an `@st.dialog("Confirm enrollment")`-decorated function taking the target prospect ids and the path label. Body: `st.write(f"Enroll {n} contact{'' if n == 1 else 's'} into the {selected_path} sequence?")` — exact locked wording. Two buttons via `st.columns(2)`: "Confirm Enrollment" (`type="primary"`) which stores the target ids in `st.session_state.pending_approval` and calls `st.rerun()`, and "Keep in Queue" (`type="secondary"`) which clears `st.session_state.pending_approval` and calls `st.rerun()` with no side effects. Both buttons fire the dialog closed; neither performs an Apollo call — Task 2 executes the approval on the rerun that follows, so the long-running calls render their spinners on the page rather than inside a modal.
6. Both approve buttons call the dialog function (never the Apollo calls directly). This is the deliberate exception to Discovery's immediate-fire button convention, justified by irreversibility — add a comment saying so and naming D-09.
7. Initialize `st.session_state.approve_result = None` and `st.session_state.pending_approval = None` in the existing session-state init block, following the `find_result`/`enrich_result` convention. Clear both whenever the path selector value changes, so one path's outcome never renders against another path's rows.
  </action>
  <verify>
    <automated>.venv/bin/python3.13 -m py_compile pages/review_queue_page.py && .venv/bin/python3.13 -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python3.13 -m py_compile pages/review_queue_page.py` exits 0 and `.venv/bin/python3.13 -m pytest -q` exits 0.
    - `grep -c "APOLLO_SEQUENCE_ID_" .streamlit/secrets.toml.example` returns 3 and `grep -c "APOLLO_SENDING_EMAIL_ACCOUNT_ID" .streamlit/secrets.toml.example` returns 1.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "@st.dialog(\"Confirm enrollment\")"` returns 1.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "Keep in Queue"` returns 1 and `grep -v '^\s*#' pages/review_queue_page.py | grep -c "Confirm Enrollment"` returns 1.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "value=True"` returns 1 — checkboxes default checked (QUEUE-03).
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "st.stop()"` returns 0.
    - `grep -v '^\s*#' app.py | grep -c "APOLLO_SEQUENCE_ID"` returns 0 — no boot gate was added.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "ensure_custom_field("` returns 1 and `grep -v '^\s*#' pages/review_queue_page.py | grep -c "opening_line_field_id"` returns at least 1.
    - `grep -v '^\s*#' app.py | grep -c "ensure_custom_field"` returns 0 — field resolution is page-scoped and lazy, never a boot-time call.
    - In `.venv/bin/python3.13 -m streamlit run app.py`: every row has a checked checkbox; unchecking one decrements the "Approve Selected (N)" count; clicking either approve button opens a modal reading "Enroll N contacts into the {Path} sequence?" with Confirm Enrollment and Keep in Queue; clicking "Keep in Queue" closes it and changes nothing.
  </acceptance_criteria>
  <done>A teammate can choose exactly which drafts to send and gets an explicit, count-accurate confirmation prompt before anything irreversible can happen, and the page has already resolved the Apollo custom field that will carry each opening line.</done>
</task>

<task type="auto">
  <name>Task 2: Approval handler — create contacts, enroll, and render per-contact Enrolled/Skipped outcomes</name>
  <files>pages/review_queue_page.py</files>
  <read_first>
    - pages/review_queue_page.py (whole file as it stands after Task 1 — the pending_approval session key, the row loop's Status cell placeholder, and the existing banner conventions)
    - pages/discovery_page.py (lines 145-265 — the try/except-wrapped external call, `st.spinner`, and session-state-result-dict conventions this handler mirrors)
    - review/logic.py and apollo/client.py and db/prospects.py (the exact signatures from 04-03)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-UI-SPEC.md (the flow-state table rows for Approve loading stages, mixed outcome, and batch hard failure; the badge colors/icons; every batch-failure copy string)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-RESEARCH.md (Pitfall 1 — 200 is not confirmation; Pitfall 5 — never blanket-advance status)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-CONTEXT.md (D-07, D-08, D-12)
  </read_first>
  <action>
Add the approval handler that runs on the rerun after the dialog's Confirm, i.e. `if st.session_state.pending_approval:` near the top of the queue section, before rendering rows. Immediately capture and clear `pending_approval` into a local variable so a subsequent rerun can never re-fire the same enrollment twice.

Handler sequence, wrapped end to end in `try/except Exception` that degrades to `st.session_state.approve_result = {"error": "Enrollment failed — check your internet connection and try again. No contacts were changed.", "enrolled": [], "skipped": []}` — never let a traceback surface and never call `st.stop()`:
1. Select the target rows from the already-loaded `rows` list by prospect id.
2. `with st.spinner("Creating Apollo contacts...")`: `payloads = build_contact_payloads(target_rows)`; `data, msg = create_contacts_bulk(apollo_key, payloads, label_names=[f"{slug.replace('_', '-')}-outreach"], opening_line_field_id=st.session_state.opening_line_field_id)` — passing the field id is what actually delivers each contact's AI opening line to Apollo (D-14/D-16); omitting it would silently send generic sequence emails. If `data is None`, write `{"error": msg, "enrolled": [], "skipped": []}` into `st.session_state.approve_result` and stop the handler — no prospect row has been touched, so the whole batch stays retryable.
3. `prospect_to_contact = map_created_contacts(data, target_rows)`. If it is empty, set an error result explaining that Apollo returned no usable contacts and stop.
3b. D-17 follow-up: `created_map, existing_map = split_contacts_by_origin(data, target_rows)`. Apollo returns already-existing contacts UNMODIFIED, so the `typed_custom_fields` sent in step 2 never reached them. For each `prospect_id, contact_id` in `existing_map`, call `update_contact_custom_field(apollo_key, contact_id, st.session_state.opening_line_field_id, opening_line)` where `opening_line` is that prospect's stored line from `target_rows`. Skip the call when the opening line is empty. Collect the prospect ids whose update returned `False` into a `personalization_failures` list — do NOT abort the batch on a failed update (enrollment is still the more important outcome) and do NOT treat it as a skip. After enrollment completes, if `personalization_failures` is non-empty, render a non-blocking `st.warning` naming how many contacts will receive the sequence email without their personalized opening line, so the teammate knows rather than finding out from a recipient.
4. Build `contact_to_prospect = {contact_id: prospect_id for prospect_id, contact_id in prospect_to_contact.items()}`.
5. `with st.spinner(f"Enrolling contacts in the {selected_path} sequence...")`: `resp, msg = add_contacts_to_sequence(apollo_key, sequence_id, list(contact_to_prospect.keys()), sending_account_id)`. If `resp is None`, write `{"error": msg, ...}` and stop — again, no prospect row has been touched (the Apollo contacts exist, but `run_dedupe: true` makes a retry return them under `existing_contacts` instead of duplicating).
6. `enrolled_ids, skipped_pairs = split_enrollment_outcome(resp, contact_to_prospect)`. This is the D-07 gate: classification comes from the response body, never from the HTTP status.
7. For each id in `enrolled_ids` ONLY: `mark_contact_created(prospect_id, prospect_to_contact[prospect_id])` then `mark_sequenced(prospect_id)`. Write no database row of any kind for a skipped prospect — that is what keeps it at `status='drafted'`, keeps it in `get_drafted_by_path`, and keeps an un-contacted person out of `contacted_registry` (D-08 plus the phase's dedup success criterion). Never loop-advance everything submitted.
8. Store `st.session_state.approve_result = {"error": None, "path": slug, "enrolled": enrolled_ids, "skipped": skipped_pairs, "personalization_failures": personalization_failures}` and call `st.rerun()` so the queue re-queries with the new statuses.

Rendering, using 04-UI-SPEC.md's exact treatments:
9. When `approve_result["error"]` is set, render `st.error(approve_result["error"], icon=":material/cancel:")` above the table.
10. When the result is clean, render a summary line above the table stating how many were enrolled and how many were skipped, then per row: if `row["id"]` is in `approve_result["enrolled"]`, replace that row's checkbox in the Select cell with `st.badge("Enrolled", icon=":material/check_circle:", color="green")` (D-12 — the badge takes the checkbox's place; the row naturally disappears on the next full reload because it is no longer `status='drafted'`). If `row["id"]` appears in `approve_result["skipped"]`, keep the interactive checkbox and render `st.badge(f"Skipped — {reason}", icon=":material/error:", color="orange")` in the Status cell, with the reason text inline in the label (not tooltip-only) and surfaced verbatim as Apollo stated it.
11. Only apply `approve_result` to the currently selected path — compare `approve_result["path"]` against the current `slug` and ignore a stale result from another path.
  </action>
  <verify>
    <automated>.venv/bin/python3.13 -m py_compile pages/review_queue_page.py && .venv/bin/python3.13 -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python3.13 -m py_compile pages/review_queue_page.py` exits 0 and `.venv/bin/python3.13 -m pytest -q` exits 0.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "split_enrollment_outcome("` returns 1.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "opening_line_field_id=st.session_state.opening_line_field_id"` returns 1 — the field id actually reaches the bulk_create call.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "split_contacts_by_origin("` returns 1 and `grep -v '^\s*#' pages/review_queue_page.py | grep -c "update_contact_custom_field("` returns 1 — the D-17 follow-up exists.
    - A failed `update_contact_custom_field` produces a non-blocking `st.warning` and does NOT move the affected contact into the skipped list (confirm by reading the handler: the failure list is separate from `skipped_pairs`).
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "mark_sequenced("` returns 1, and the only `mark_sequenced` call site is inside a loop over `enrolled_ids` (confirm by reading the surrounding lines — a loop over all target rows fails this criterion).
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c 'color="green"'` returns 1 and `grep -v '^\s*#' pages/review_queue_page.py | grep -c 'color="orange"'` returns 1.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -cE "status_code|== 200"` returns 0 — the page never inspects an HTTP status; classification comes from the response body only.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "Creating Apollo contacts..."` returns 1 and `grep -v '^\s*#' pages/review_queue_page.py | grep -c "Enrolling contacts in the"` returns 1.
    - `.venv/bin/python3.13 -c "import ast,sys; t=ast.parse(open('pages/review_queue_page.py').read()); print('parsed ok')"` prints `parsed ok`.
    - With a deliberately invalid `APOLLO_SEQUENCE_ID_*` value in `.streamlit/secrets.toml`, confirming an enrollment renders a red banner carrying Apollo's own 422 message and leaves every row still checked and still in the queue on reload.
  </acceptance_criteria>
  <done>Confirming an approval creates Apollo contacts, enrolls them, and reports each contact's real outcome — enrolled rows badge green and leave the queue, skipped rows stay retryable with Apollo's reason shown.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python3.13 -m pytest -q` green (always `python3.13` — 04-RESEARCH.md Pitfall 4).
- Unchecking a row and clicking "Approve Selected" enrolls only the checked rows; the unchecked row is still present and still `status='drafted'` after a reload.
- A hard Apollo failure leaves `SELECT COUNT(*) FROM prospect WHERE status='sequenced'` unchanged.
</verification>

<success_criteria>
- QUEUE-03: default-checked per-row checkboxes; both Approve Selected and Approve All enroll exactly the intended set; unchecked contacts stay `drafted`.
- QUEUE-04: contacts created with `run_dedupe: true` and enrolled into the path's pre-built sequence.
- D-07: no contact is reported Enrolled without appearing in Apollo's response `contacts` array.
- D-08/D-12: partial failure does not block the batch; per-contact outcomes are visible in place.
- D-14/D-16/D-17: every approved contact's opening line reaches Apollo — inside the create call for new contacts, via a follow-up update for pre-existing ones — and any failure to deliver it is surfaced, never silent.
- Phase success criterion 4: enrolled prospects carry an `apollo_contact_id` and therefore appear in `contacted_registry` immediately.
</success_criteria>

<output>
Create `.planning/phases/04-review-queue-and-sequence-enrollment/04-04-SUMMARY.md` when done.
</output>
