---
phase: 04-review-queue-and-sequence-enrollment
plan: 05
type: execute
wave: 5
depends_on: ["04-04"]
files_modified:
  - apollo/client.py
  - review/logic.py
  - pages/review_queue_page.py
  - tests/test_apollo_client.py
  - tests/test_review_logic.py
autonomous: false
requirements: [QUEUE-03, QUEUE-04]

must_haves:
  truths:
    - "The three Apollo sequence IDs and the sending mailbox ID are real values resolved from this team's Apollo account, not placeholders"
    - "The live bulk_create and add_contact_ids response shapes have been observed against a real Apollo call and every field name the code reads is confirmed or corrected"
    - "A real contact was enrolled end to end from the Review Queue and verified present in the Apollo sequence in Apollo's own UI"
    - "Any mismatch between the documented response shape and the live response is fixed in code and recorded with a CONFIRMED comment, matching this repo's 02-03 convention"
    - "D-14: all 3 Apollo sequences have had their email step set up by hand with the static template text plus the AI Opening Line merge tag, and a real contact preview shows the opening line resolved rather than a raw {{...}} token"
    - "A6: the custom field is confirmed NOT required in Apollo's field settings, so a contact with a blank opening line still receives its sequence email"
  artifacts:
    - path: "apollo/client.py"
      provides: "live-confirmed request/response handling for both Phase 4 endpoints"
      contains: "CONFIRMED (04-05"
    - path: "review/logic.py"
      provides: "live-confirmed skipped-reason parsing"
      contains: "CONFIRMED (04-05"
  key_links:
    - from: "apollo/client.py"
      to: "Apollo live response"
      via: "field names reconciled against an observed real payload"
      pattern: "CONFIRMED \\(04-05"
    - from: "Apollo sequence email step"
      to: "AI Opening Line custom field merge tag"
      via: "one-time manual setup per sequence, verified in Apollo's own preview"
      pattern: "AI Opening Line"
---

<objective>
Close the two live-verification gaps this phase was built on top of: the four Apollo IDs nobody has resolved yet, and the `bulk_create` / `add_contact_ids` response shapes that were taken from documentation rather than a real call.

Purpose: This repo's established convention (see the `CONFIRMED (02-03 live human-verify...)` comments in `apollo/client.py` and `db/prospects.py`) is that no new Apollo endpoint ships on documentation alone. Phase 2 found two documented-but-wrong field names this way; 04-RESEARCH.md flags the Phase 4 reason-code vocabulary as the same class of risk.
Output: real secret values in `.streamlit/secrets.toml`, the one-time manual merge-tag setup in all 3 Apollo sequences (D-14), a verified end-to-end enrollment whose email actually renders the AI opening line, and code reconciled to the observed live payloads with dated CONFIRMED comments.
</objective>

## Phase Goal

**As a** non-technical teammate running an outreach campaign, **I want to** review the full queue of drafted emails and approve them in bulk, **so that** the contacts get enrolled into the correct Apollo sequence and the campaign goes live.

This plan proves the story is true against the real Apollo account.

<execution_context>
@/Users/yashpersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yashpersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-CONTEXT.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-RESEARCH.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-04-SUMMARY.md
@spec.MD

<interfaces>
<!-- Assumptions under test in this plan, from 04-RESEARCH.md's Assumptions Log. -->

A1  add_contact_ids skipped_contact_ids is an object keyed by contact id whose values are reason-code
    strings such as "contacts_active_in_other_campaigns" / "contacts_finished_in_other_campaigns".
    NOT independently re-confirmed. spec.MD's `sequence_active_in_other_campaigns` names are REQUEST
    override flags, a different vocabulary — do not conflate (04-RESEARCH.md Pitfall 2).
A2  bulk_create splits its 200 body into "created_contacts" and "existing_contacts" arrays, each entry
    carrying "id" and "email". Email is the reconciliation key used by review.logic.map_created_contacts.
A3  The sending mailbox is one shared account across all three paths, hardcoded as
    APOLLO_SENDING_EMAIL_ACCOUNT_ID (D-13).
A5  meta.max_length 500 is sufficient headroom for the opening line. Low risk.
A6  A custom field created via POST /fields (no `required` param sent) defaults to NOT required and
    will not block a sequence send for a contact whose opening line is blank. MEDIUM risk — if wrong,
    an entire sequence email is blocked rather than merely unpersonalized. Must be confirmed in
    Apollo's field-settings UI during this checkpoint, not assumed.
A7  The `{ }` dynamic-variable picker steps in 04-RESEARCH.md §5 match the current live Apollo editor.
    Sourced via search summaries (the knowledge-base pages 403'd on direct fetch) — the human operator
    self-corrects live if the UI differs; no code change follows from a wording mismatch.

Lookup endpoints for the ID discovery step:
  GET  {APOLLO_BASE}/email_accounts          -> each linked mailbox's id / email / active / default
  POST {APOLLO_BASE}/emailer_campaigns/search -> each sequence's id and name
  GET  {APOLLO_BASE}/fields?source=custom     -> each custom field's prefixed id / label / modality

Precedent to match, verbatim comment style:
  apollo/client.py lines 164-173 — "CONFIRMED (02-03 live human-verify, 2026-09-11): ..." followed by
  what was confirmed, what was wrong, and what was changed as a result.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the real Apollo sequence IDs, sending mailbox ID, and custom-field state</name>
  <files>(no source files modified — read-only Apollo lookups)</files>
  <read_first>
    - apollo/client.py (APOLLO_BASE and the `x-api-key` header convention)
    - .streamlit/secrets.toml (the live local secrets file — confirm APOLLO_API_KEY is present; never print its value)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-CONTEXT.md (D-01, D-02, D-13 — why these are static secrets rather than live lookups)
  </read_first>
  <action>
Run two read-only Apollo lookups using the existing `APOLLO_API_KEY` from `.streamlit/secrets.toml`, loading it with `tomllib` inside a single `.venv/bin/python3.13 -c` invocation. Do not create a script file, do not write anything to disk, and never print, echo, or log the API key itself.

1. `GET https://api.apollo.io/api/v1/email_accounts` with the `x-api-key` header. Print one line per mailbox: `id`, `email`, `active`, `default`. The candidate for `APOLLO_SENDING_EMAIL_ACCOUNT_ID` is the active mailbox on the project's sending domain (`SENDING_DOMAIN` in the same secrets file).
2. `POST https://api.apollo.io/api/v1/emailer_campaigns/search` with the `x-api-key` header and an empty JSON body. Print one line per sequence: `id`, `name`, and `active`/status if present. These are the candidates for the three `APOLLO_SEQUENCE_ID_*` secrets.
3. `GET https://api.apollo.io/api/v1/fields?source=custom` with the `x-api-key` header. Print one line per custom field: `id` exactly as returned, `label`, and `modality`. State explicitly whether a contact-modality field labelled "AI Opening Line" already exists, and print both its raw returned id and the prefix-stripped form (`id.split(".", 1)[-1]`) so the developer can see the two differ — this is the gotcha the regression test in 04-06 guards. Do not create the field here; `ensure_custom_field()` creates it on first load of the Review Queue page.

If either call returns a non-200, print the status code and `response.json().get("message")` and stop — do not retry in a loop, and report the failure to the developer rather than guessing IDs. A 403 here means the key is not a Master key (sequences and contacts both require one).

Present the results as two small tables and state plainly which value you believe maps to each of the four secret keys, based on mailbox domain and sequence name matching the three path names (Club Sponsorship, Productthon Sponsorship, Client Sourcing). Do not write any value into `.streamlit/secrets.toml` yourself — the developer confirms the mapping in Task 2.
  </action>
  <verify>
    <automated>.venv/bin/python3.13 -c "import tomllib,requests; k=tomllib.load(open('.streamlit/secrets.toml','rb'))['APOLLO_API_KEY']; r=requests.get('https://api.apollo.io/api/v1/email_accounts', headers={'x-api-key':k}, timeout=15); print('status', r.status_code)"</automated>
  </verify>
  <acceptance_criteria>
    - The `email_accounts` call prints `status 200` and at least one mailbox row with a non-empty `id`.
    - The `emailer_campaigns/search` call prints at least one sequence row with a non-empty `id` and `name`.
    - The `fields?source=custom` call prints `status 200` and states whether an "AI Opening Line" contact field already exists; if one exists, both its prefixed and prefix-stripped ids are shown.
    - No command output anywhere in this task contains the Apollo API key value.
    - The task output names a specific candidate value for each of the four secret keys, or states explicitly which one could not be resolved and why.
  </acceptance_criteria>
  <done>Real candidate values for all four Phase 4 secrets are on screen, sourced from the team's live Apollo account.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — paste the four secrets, run one real enrollment, capture the live payloads</name>
  <files>.streamlit/secrets.toml (developer-edited, gitignored — not a source change)</files>
  <read_first>
    - Task 1's printed mailbox and sequence tables (the candidate values being pasted)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-UI-SPEC.md (the Approve Action & Confirmation Contract and the badge treatments being confirmed visually)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-RESEARCH.md (Assumptions Log A1/A2/A3 — the claims this checkpoint exists to settle)
  </read_first>
  <what-built>
The Review Queue page is complete: path-scoped queue, shared template editor with merge-field validation, default-checked per-row selection, a confirmation dialog, and an approval flow that creates Apollo contacts with `run_dedupe: true`, attaches each contact's AI opening line as an Apollo custom field (D-14/D-16), applies it via a follow-up update for contacts Apollo already had (D-17), and enrolls them into the path's pre-built sequence — reporting each contact as Enrolled or Skipped based on Apollo's response body. Task 1 has just printed the real mailbox IDs, sequence IDs, and current custom-field state from your Apollo account.

What the app cannot do for you: Apollo sequences are authored in Apollo's own editor, so the static paragraph text and the merge tags inside each of the 3 sequence email steps are a one-time manual setup (D-14, same category as the `[SPONSORSHIP_LINK]` placeholder from Phase 3). Steps 2-4 below walk you through it.
  </what-built>
  <how-to-verify>
1. Open `.streamlit/secrets.toml` (gitignored — it is the only place these belong) and paste the four values from Task 1's output:
   `APOLLO_SEQUENCE_ID_CLUB_SPONSORSHIP`, `APOLLO_SEQUENCE_ID_PRODUCTTHON`, `APOLLO_SEQUENCE_ID_CLIENT_SOURCING`, `APOLLO_SENDING_EMAIL_ACCOUNT_ID`.
2. Run `.venv/bin/python3.13 -m streamlit run app.py` and open **Review Queue**. Confirm the missing-secret warning is gone and the approve buttons are enabled. On this first load the app calls `ensure_custom_field()`, which creates the **"AI Opening Line"** contact field in Apollo if it does not already exist. If you see the red "approving is paused" banner instead, report its exact wording — the field did not resolve.
3. **Confirm the field is not required (Assumption A6 — the highest-risk unknown in this phase).** In Apollo, go to Settings → Fields (or Data → Custom Fields) and open **"AI Opening Line"**. Confirm it is NOT marked required. Apollo blocks a sequence email from sending when a *required* dynamic variable has no value for a contact — if this field were required, any contact whose opening line is blank (Phase 3's fallback path can produce one) would have its entire email silently blocked rather than just sent unpersonalized. If Apollo shows it as required, turn that off and say so in your report.
4. **One-time sequence setup, repeat for all 3 sequences** (Club Sponsorship, Productthon Sponsorship, Client Sourcing). The exact UI wording below is MEDIUM confidence (sourced from Apollo's knowledge base via search summaries) — if the live editor differs, follow the equivalent control and tell us what it actually looked like:
   a. Open the sequence and click into its first email step.
   b. Paste in the static paragraph text for that path from `personalization/templates.py`'s `PATH_TEMPLATES` (subject and body), if it is not already there.
   c. Delete the literal `{opening_line}` placeholder text, put the cursor where it sat, click the **`{ }`** button in the editor toolbar, find **"AI Opening Line"** under the custom-fields group, and insert it.
   d. Replace the template's `{first_name}` and `{company}` placeholders with Apollo's own native **`{{first_name}}`** and **`{{company_name}}`** basic variables from the same `{ }` picker — these need no custom field, Apollo already knows them.
   e. Use the sequence's preview against a real contact and confirm all three merge points resolve to real values, not raw `{{...}}` tokens. Save the email step.
5. Pick a path that has at least one drafted contact. **Uncheck every row except one** — this first live run should enroll exactly one real person. Prefer a contact you are comfortable actually emailing, or a test contact at your own domain.
6. Click **Approve Selected (1)**. Confirm the dialog reads "Enroll 1 contact into the {Path} sequence?" with **Confirm Enrollment** and **Keep in Queue**.
7. Click **Keep in Queue** first. Confirm nothing happened: the row is still checked, still listed, no banner.
8. Click **Approve Selected (1)** again, then **Confirm Enrollment**. Watch for both spinners ("Creating Apollo contacts..." then "Enrolling contacts in the {Path} sequence...").
9. Report back:
   - the exact banner/badge you saw (green **Enrolled**, or orange **Skipped — {reason}**, or a red error banner and its wording);
   - the **raw JSON** of both Apollo responses. To capture them, re-run the two calls directly with the same contact, or add a temporary `print(resp.json())` — paste the literal payloads, especially the exact key names inside `created_contacts` / `existing_contacts` and the exact structure and reason strings inside `skipped_contact_ids`.
10. In the Apollo UI, open that sequence and confirm the contact is actually listed as an active member. A green badge in our app that Apollo's own UI does not corroborate is a failure, not a pass.
11. Run `.venv/bin/python3.13 -c "import sqlite3; c=sqlite3.connect('db/outreach.db'); print(list(c.execute(\"SELECT id,status,apollo_contact_id FROM prospect WHERE status IN ('sequenced','contact_created','drafted') ORDER BY id DESC LIMIT 10\")))"` and paste the output — the enrolled contact must be `sequenced` with a non-NULL `apollo_contact_id`, and every unchecked contact must still be `drafted` with a NULL `apollo_contact_id`.
12. **The personalization check (D-14 — the reason this whole custom-field design exists).** Open that same contact's record in Apollo and confirm the **"AI Opening Line"** field holds the exact opening line the Review Queue showed for them. Then open the sequence's email preview for that contact and confirm the opening line renders inline in the body — not blank, not a raw `{{...}}` token, and not the generic template alone. A contact enrolled with an empty custom field means the personalization pipeline silently failed and this checkpoint is a FAIL even if enrollment succeeded.
13. If you have a contact that Apollo already knew about (one that came back under `existing_contacts` rather than `created_contacts`), verify its "AI Opening Line" field too — that path goes through the D-17 follow-up update rather than the create call, and it is the one most likely to silently miss. If the app showed the orange "will receive the sequence email without their personalized opening line" warning, paste it verbatim.
14. Finally, a known consequence worth confirming you are happy with: editing the shared template in the app's Review Queue editor now changes only the **preview and the opening-line generation context** — the static paragraphs that actually send live in Apollo's sequence editor (step 4b). If you edit the template in the app, you must re-paste it into the Apollo sequences. Say whether that tradeoff is acceptable or whether you want a follow-on capability to push template text into Apollo.
  </how-to-verify>
  <action>
Pause for the developer. Do not modify any source file during this checkpoint. Wait for an explicit approval plus the two raw Apollo JSON payloads and the SQL output before starting Task 3 — Task 3's reconciliation has no input without them. If the developer reports a failure instead, fix the specific cause in Task 3 rather than re-running this checkpoint blind.
  </action>
  <verify>
    <automated>.venv/bin/python3.13 -c "import sqlite3; c=sqlite3.connect('db/outreach.db'); print('sequenced:', list(c.execute(\"SELECT COUNT(*) FROM prospect WHERE status='sequenced' AND apollo_contact_id IS NOT NULL\"))[0][0])"</automated>
    <human-check>The enrolled contact is visible as an active member of the correct sequence in Apollo's own UI, and the raw JSON of both Apollo responses has been pasted back.</human-check>
  </verify>
  <acceptance_criteria>
    - The four secrets are present in `.streamlit/secrets.toml` and the Review Queue page no longer shows the missing-secret warning.
    - Clicking "Keep in Queue" produced no banner, no status change, and no Apollo call.
    - Confirming the enrollment produced a visible per-contact outcome (green Enrolled badge, orange Skipped badge with a reason, or a red error banner with Apollo's own message) — never a silent success.
    - The `sequenced` count query above returns at least 1, and every contact left unchecked is still `drafted` with a NULL `apollo_contact_id`.
    - The developer corroborated the enrollment inside the Apollo UI.
    - The raw JSON payloads for both `bulk_create` and `add_contact_ids` were pasted back verbatim.
    - The "AI Opening Line" field exists in Apollo, is confirmed NOT required (A6), and holds the expected text on the enrolled contact's record.
    - The sequence email preview for that contact renders the opening line inline — an enrolled contact with a blank custom field is a FAIL, not a pass.
    - All 3 sequences have had their email step set up with the static text plus the merge tags (D-14), or the developer stated explicitly which ones were deferred and why.
  </acceptance_criteria>
  <done>The developer confirmed a real end-to-end enrollment against the live Apollo account, verified the AI opening line actually reaches the contact record and renders in the sequence email, and supplied the raw response payloads Task 3 needs — or reported the exact failure to fix.</done>
  <resume-signal>Reply "approved" plus the two raw JSON payloads, the SQL output, the custom-field required/not-required finding, and what the sequence preview rendered — or describe what went wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Reconcile the code against the observed live payloads</name>
  <files>apollo/client.py, review/logic.py, pages/review_queue_page.py, tests/test_apollo_client.py, tests/test_review_logic.py</files>
  <read_first>
    - The raw JSON payloads and the SQL output the developer pasted in Task 2 — these are the source of truth, outranking 04-RESEARCH.md and spec.MD wherever they disagree
    - apollo/client.py (lines 159-194 — the exact `CONFIRMED (02-03 live human-verify, 2026-09-11)` docstring comment style to replicate, including naming what was confirmed, what was wrong, and what changed)
    - review/logic.py (map_created_contacts and split_enrollment_outcome — the two functions whose field-name assumptions are under test)
    - pages/review_queue_page.py (the badge-rendering block, in case a reason string renders badly)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-RESEARCH.md (Assumptions Log A1/A2 and Pitfall 2)
  </read_first>
  <action>
Compare every field name the code reads against the pasted live payloads and fix each mismatch at its source:
1. `create_contacts_bulk` — if the 200 body uses different wrapper keys than `created_contacts` / `existing_contacts`, or if entries carry the email under a different key, correct `review.logic.map_created_contacts` (not the client — the client returns the raw body by design).
2. `add_contacts_to_sequence` / `split_enrollment_outcome` — confirm the enrolled array is `contacts` and each entry's id key is `id`; confirm whether `skipped_contact_ids` is an id→reason object, a bare list of ids, or absent when nothing was skipped, and make the parsing match what actually arrived. If the live reason strings differ from the documented `contacts_*` vocabulary, change nothing about how they are displayed (04-UI-SPEC.md requires verbatim reason text) but record the real observed strings in a comment.
3. Custom-field delivery (D-14 through D-18): confirm against the live evidence that (a) `GET /fields` really did return the modality-prefixed id form and the prefix strip in `_find_custom_field_id` was necessary — if the live response returns a raw id instead, keep the strip (it is a no-op on an unprefixed string) but record the observation; (b) the raw id sent in `typed_custom_fields` was accepted and the value landed on the contact record; (c) the D-17 follow-up `PATCH /contacts/{id}` succeeded for any contact returned under `existing_contacts`. If the opening line did NOT land on a contact record, that is the failure to fix here — most likely causes in order: a prefixed id used as a `typed_custom_fields` key, `opening_line` leaking as a top-level contact attribute, or the field id resolving to an account-modality field.
4. If enrollment failed with a 422 naming a missing or malformed parameter, fix the request construction — most likely the `contact_ids[]` query-param form or the `emailer_campaign_id` pairing — and note the corrected form.

Record the outcome in the repo's established style: add a `CONFIRMED (04-05 live human-verify, <today's date>): ...` block to the docstrings of `create_contacts_bulk`, `add_contacts_to_sequence`, `_find_custom_field_id` (state the live id format observed and whether the prefix strip fired) and `update_contact_custom_field`, and to `split_enrollment_outcome`, each stating what the live call confirmed, what (if anything) the documentation had wrong, and what was changed as a result. If everything matched exactly, say that explicitly — a confirmation with no corrections is still the record future phases need.

Update any unit test in `tests/test_apollo_client.py` or `tests/test_review_logic.py` whose mocked payload no longer matches the real shape, so the suite encodes the confirmed reality rather than the documented guess. Do not weaken an assertion to make a test pass — correct the fixture payload.
  </action>
  <verify>
    <automated>.venv/bin/python3.13 -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python3.13 -m pytest -q` exits 0.
    - `grep -c "CONFIRMED (04-05" apollo/client.py` returns at least 4 (bulk_create, add_contact_ids, the field lookup, and the contact PATCH).
    - `grep -c "CONFIRMED (04-05" review/logic.py` returns at least 1.
    - Every mocked response payload in `tests/test_apollo_client.py` for the two Phase 4 endpoints uses the key names observed in the live payloads pasted in Task 2.
    - If any correction was made, the SUMMARY names the exact field or parameter that was wrong and where it was fixed.
  </acceptance_criteria>
  <done>The Phase 4 Apollo integration is verified against a real account, and the code plus its tests encode the observed reality with a dated confirmation comment.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python3.13 -m pytest -q` green.
- At least one real contact is visible as an active member of the correct sequence inside Apollo's own UI.
- `SELECT status, apollo_contact_id FROM prospect` shows `sequenced` + a contact id for that person, and unchanged `drafted` + NULL for every contact that was not approved.
</verification>

<success_criteria>
- All four Phase 4 secrets hold real values from this team's Apollo account (D-01, D-02, D-13).
- ROADMAP success criterion 3 is met in the strong sense: enrollment is confirmed by Apollo's response body and corroborated in Apollo's UI, not inferred from a 200.
- ROADMAP success criterion 4 is met: the enrolled contact and its company domain are in `contacted_registry` immediately.
- 04-RESEARCH.md Assumptions A1, A2, A3, A5, A6 and A7 are each resolved to confirmed-or-corrected, with the record in code (A6 in particular — a required custom field would block sends outright, not merely drop personalization).
- D-14: the AI opening line is verified present on a real Apollo contact record AND rendered in that sequence's email preview — personalization is no longer preview-only.
</success_criteria>

<output>
Create `.planning/phases/04-review-queue-and-sequence-enrollment/04-05-SUMMARY.md` when done.
</output>
