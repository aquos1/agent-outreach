---
phase: 04-review-queue-and-sequence-enrollment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/schema.py
  - db/prospects.py
  - tests/test_schema.py
  - tests/test_prospects.py
  - pages/review_queue_page.py
  - app.py
autonomous: true
requirements: [QUEUE-01, QUEUE-02]

must_haves:
  truths:
    - "A teammate can reach a new 'Review Queue' page from the app's left navigation"
    - "D-11: The Review Queue is scoped to one outreach path at a time via a selector that always has a path selected (no 'nothing chosen' state)"
    - "QUEUE-01/QUEUE-02: Every contact at status='drafted' for the selected path appears in the queue with Name, Company and Title visible"
    - "D-10: Each queued row has a collapsed 'View draft' expander showing the full assembled subject + body for that contact"
    - "A path with zero drafted contacts shows a neutral empty-state message (st.info), never an error or warning"
    - "The page never renders a Streamlit traceback and never hard-stops, even if the database read fails"
  artifacts:
    - path: "pages/review_queue_page.py"
      provides: "Review Queue page: path selector, queue table rows, per-row draft expander, empty state"
      contains: "get_drafted_by_path"
      min_lines: 90
    - path: "db/prospects.py"
      provides: "get_drafted_by_path() queue read query"
      contains: "def get_drafted_by_path"
    - path: "db/schema.py"
      provides: "template_override table + title/last_name additive columns on prospect"
      contains: "template_override"
    - path: "app.py"
      provides: "Review Queue nav registration"
      contains: "review_queue_page.py"
  key_links:
    - from: "pages/review_queue_page.py"
      to: "db.prospects.get_drafted_by_path"
      via: "queue read scoped to the selected path slug"
      pattern: "get_drafted_by_path\\("
    - from: "pages/review_queue_page.py"
      to: "personalization.templates.assemble_email"
      via: "per-row full draft assembly for the expander preview"
      pattern: "assemble_email\\("
    - from: "app.py"
      to: "pages/review_queue_page.py"
      via: "st.navigation pages dict entry"
      pattern: "review_queue_page\\.py"
---

<objective>
Deliver the first vertical slice of Phase 4: a teammate can open a new Review Queue page, pick an outreach path, and see every drafted email waiting for review — contact name, company, role, and the full assembled email behind a per-row expander.

Purpose: QUEUE-01 and QUEUE-02 exist entirely for this moment — until the teammate can *see* the queue, nothing else in this phase is usable. This plan also lands every schema change the rest of the phase needs (one migration touch, not four).
Output: `pages/review_queue_page.py` (new, read-only), `get_drafted_by_path()` in `db/prospects.py`, `title`/`last_name` columns + the `template_override` table in `db/schema.py`, and a nav entry in `app.py`.
</objective>

## Phase Goal

**As a** non-technical teammate running an outreach campaign, **I want to** review the full queue of drafted emails and approve them in bulk, **so that** the contacts get enrolled into the correct Apollo sequence and the campaign goes live.

This plan delivers the "review the full queue" half of that story.

<execution_context>
@/Users/yashpersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yashpersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-CONTEXT.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-RESEARCH.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-PATTERNS.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs. All of these already exist in the codebase. -->

db/schema.py (existing):
  DDL: str                      # one triple-quoted CREATE TABLE IF NOT EXISTS block per table
  VIEW_DDL: str                 # contacted_registry view; WHERE p.apollo_contact_id IS NOT NULL
  _NEW_COLUMNS: list[tuple[str, str]]   # currently [("opening_line","TEXT"),("first_name","TEXT"),("draft_source","TEXT")]
  _migrate_columns(conn) -> None        # swallows "duplicate column name" only
  ensure_schema(db_path: str = "db/outreach.db") -> None

db/prospects.py (existing):
  _domain_from_url(url) -> str | None
  _domain_from_email(email) -> str | None
  dedup_filter(candidates, db_path=...) -> list[dict]     # the file's one existing SELECT, follow its lifecycle
  insert_enriched(rows: list[dict], path: str, db_path=...) -> int
  update_draft(prospect_id, opening_line, source, first_name=None, db_path=...) -> None

prospect table columns today:
  id INTEGER PK, name, company, company_domain, apollo_person_id, apollo_contact_id,
  email, path, status (CHECK found|selected|enriched|contact_created|drafted|approved|sequenced),
  sequence_id, created_at, updated_at, opening_line, first_name, draft_source
  -- NOTE: there is NO `title` column and NO `last_name` column today. This plan adds both.

discovery/logic.py (existing):
  PATH_OPTIONS = ["Club Sponsorship", "Productthon Sponsorship", "Client Sourcing"]
  PATH_CONFIG  = {"Club Sponsorship": {"slug": "club_sponsorship"},
                  "Productthon Sponsorship": {"slug": "productthon"},
                  "Client Sourcing": {"slug": "client_sourcing"}}

personalization/templates.py (existing):
  PATH_TEMPLATES: dict[str, dict]                      # keys: club_sponsorship | productthon | client_sourcing
  assemble_email(path_slug, first_name, company, opening_line) -> tuple[str, str]   # (subject, body), pure

app.py (existing):
  pages = {"Health": [st.Page("pages/health_page.py", title="System Health", default=True)],
           "Discovery": [st.Page("pages/discovery_page.py", title="Contact Discovery")]}
  pg = st.navigation(pages); pg.run()

Apollo bulk_match match dict (live-confirmed 02-03) fields relevant here:
  match["id"], match["name"], match["first_name"], match["last_name"], match["title"],
  match["email"], match["organization"]["name"], match["organization"]["website_url"]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Queue data layer — title/last_name columns, template_override table, and get_drafted_by_path()</name>
  <files>db/schema.py, db/prospects.py, tests/test_schema.py, tests/test_prospects.py</files>
  <read_first>
    - db/schema.py (whole file — the `DDL` string at lines 15-51, `VIEW_DDL` at 56-72, `_NEW_COLUMNS` at 79-83, `_migrate_columns` at 86-98, `ensure_schema` at 101-119)
    - db/prospects.py (whole file — `dedup_filter` at 37-80 is the one existing SELECT and the lifecycle to copy; `insert_enriched` at 83-129 gains new columns; `update_draft` at 132-158 is the status-transition shape)
    - tests/test_schema.py (`test_ensure_schema_idempotent` at 9-33, `test_persistence_across_reconnect` at 79-105, `test_column_migration_idempotent` at 108-159)
    - tests/test_prospects.py (`test_insert_enriched_writes_enriched_status` at 76-119, `test_update_draft_writes_status_drafted` at 121-179 — note the deferred-import-inside-test-body convention)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-PATTERNS.md (sections "db/schema.py (migration, extend)", "db/prospects.py (model/service, extend)", "tests/test_schema.py (extend)", "tests/test_prospects.py (extend)")
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-RESEARCH.md (Pattern 4 for the template_override DDL; Anti-Patterns — do NOT add the new table to _NEW_COLUMNS)
  </read_first>
  <behavior>
    - tests/test_schema.py::test_ensure_schema_idempotent — after two ensure_schema() calls, the sqlite_master table set includes `template_override` alongside `prospect`, `email_events`, `free_email_domains` (extend the existing IN (...) list and the asserted set).
    - tests/test_schema.py::test_template_override_roundtrip — ensure_schema on a temp db, raw INSERT of ('club_sponsorship', 'S', 'B'), reconnect, SELECT returns subject 'S' and body 'B'; a second ensure_schema() call does not raise and does not drop the row.
    - tests/test_schema.py::test_column_migration_idempotent — extend the existing assertion so the migrated prospect column set also contains `title` and `last_name`.
    - tests/test_prospects.py::test_insert_enriched_persists_title_and_names — seed a match dict carrying `title`, `first_name`, `last_name`; after insert_enriched, raw SELECT of title/first_name/last_name returns those exact values.
    - tests/test_prospects.py::test_get_drafted_by_path — seed 3 rows: two `club_sponsorship` advanced to status='drafted' via update_draft, one `productthon` also drafted, plus one `club_sponsorship` left at status='enriched'. get_drafted_by_path("club_sponsorship") returns exactly the two drafted club rows, each a dict carrying keys id, name, first_name, last_name, company, company_domain, email, title, path, opening_line. The enriched row and the productthon row are absent.
    - tests/test_prospects.py::test_get_drafted_by_path_empty — a path with no drafted rows returns an empty list (not None, no exception).
  </behavior>
  <action>
Write the tests above FIRST and confirm they fail, then implement.

In `db/schema.py`:
1. Append a `template_override` table to the existing `DDL` triple-quoted string (inside the same string, after the `free_email_domains` INSERT OR IGNORE block — keep the single-`DDL`-string convention). Columns exactly: `path TEXT PRIMARY KEY`, `subject TEXT NOT NULL`, `body TEXT NOT NULL`, `updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP`. Add an inline comment naming the three valid path slugs (`club_sponsorship` | `productthon` | `client_sourcing`) and referencing D-05. This is a brand-new table — `CREATE TABLE IF NOT EXISTS` alone is idempotent; do NOT add its columns to `_NEW_COLUMNS` and do NOT invent a migration runner.
2. Add `("title", "TEXT")` and `("last_name", "TEXT")` to `_NEW_COLUMNS`. Both are additive ALTER TABLE migrations onto the already-populated `prospect` table (rows drafted during Phase 3 will carry NULL for both — downstream display code must tolerate NULL). Update the module docstring's migration sentence to name the new columns and say why (`title` backs QUEUE-02's role column; `last_name` backs Phase 4's Apollo contact-creation payload).

In `db/prospects.py`:
3. Extend `insert_enriched`'s INSERT to also persist `title`, `first_name`, and `last_name`, read defensively as `match.get("title")`, `match.get("first_name")`, `match.get("last_name")`. Keep the existing `?`-placeholder style — never f-string/`.format`/`%` interpolation of Apollo-derived values into SQL. Note in the docstring that `update_draft` later overwrites `first_name` with the same value and that this is harmless.
4. Add `get_drafted_by_path(path_slug: str, db_path: str = "db/outreach.db") -> list[dict]`. Follow `dedup_filter`'s lifecycle exactly: `sqlite3.connect` → `cur.execute(SELECT ...)` → `fetchall()` inside `try/finally: conn.close()` → build the return value outside the connection block. Query: `SELECT id, name, first_name, last_name, company, company_domain, email, title, path, opening_line FROM prospect WHERE status = 'drafted' AND path = ? ORDER BY id`. Return a list of plain dicts keyed by those exact column names (explicit unpacking or a `cur.description`-driven zip — not raw tuples, not sqlite3.Row objects). Docstring must state that this is the Review Queue's source query (QUEUE-01) and that skipped contacts intentionally stay visible here because they remain at status='drafted' (D-08).
  </action>
  <verify>
    <automated>.venv/bin/python3.13 -m pytest tests/test_schema.py tests/test_prospects.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python3.13 -m pytest tests/test_schema.py tests/test_prospects.py -q` exits 0 with the six behaviors above passing.
    - `.venv/bin/python3.13 -m pytest -q` (full suite) exits 0 — no Phase 1-3 test regressions.
    - `grep -v '^\s*#' db/schema.py | grep -c "template_override"` returns at least 1.
    - `grep -v '^\s*#' db/schema.py | grep -c '("title", "TEXT")'` returns 1 and `grep -v '^\s*#' db/schema.py | grep -c '("last_name", "TEXT")'` returns 1.
    - `grep -c "template_override" <(grep -A6 "_NEW_COLUMNS = \[" db/schema.py)` returns 0 — the new table is NOT in the ALTER TABLE migration list.
    - `grep -v '^\s*#' db/prospects.py | grep -c "def get_drafted_by_path"` returns 1.
    - `.venv/bin/python3.13 -c "import sqlite3,tempfile,os; from db.schema import ensure_schema; p=os.path.join(tempfile.mkdtemp(),'t.db'); ensure_schema(p); ensure_schema(p); c=sqlite3.connect(p); print(sorted(r[1] for r in c.execute('PRAGMA table_info(prospect)')))"` prints a list containing both `title` and `last_name`.
  </acceptance_criteria>
  <done>The prospect table carries title/last_name on every boot, the template_override table exists, and get_drafted_by_path returns path-scoped drafted rows as dicts — all covered by passing unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Review Queue page — path selector, queue rows, per-row draft expander, empty state, nav registration</name>
  <files>pages/review_queue_page.py, app.py</files>
  <read_first>
    - pages/discovery_page.py (whole file — module docstring rules at lines 1-33, the secrets read at 61-64, session-state init at 66-71, the selectbox at 73-75, and the table + expander render block at 295-323 which this page adapts)
    - app.py (whole file — the secrets gate at 27-43 and the `pages` dict at 47-52)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-UI-SPEC.md (sections "Layout & Review Queue Flow State System", "Queue Table Contract", and the "Copywriting Contract" — every string and widget on this page is locked there)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-PATTERNS.md (section "pages/review_queue_page.py (controller, new)" and "app.py (config, extend)")
    - db/prospects.py (the get_drafted_by_path signature and returned dict keys from Task 1)
    - personalization/templates.py (assemble_email signature and PATH_TEMPLATES keys)
    - discovery/logic.py (PATH_OPTIONS / PATH_CONFIG — reuse, never re-derive a new label set)
  </read_first>
  <action>
Create `pages/review_queue_page.py` following `pages/discovery_page.py`'s structure exactly: module docstring first (state the page's purpose, the D-10/D-11 layout decisions, the never-hard-stop rule, and the line `NEVER log or print the api_key`), then `from __future__ import annotations`, then `import streamlit as st`, then project imports (`PATH_CONFIG`, `PATH_OPTIONS` from `discovery.logic`; `get_drafted_by_path` from `db.prospects`; `assemble_email` from `personalization.templates`).

Page body, top to bottom, using the exact copy from 04-UI-SPEC.md's Copywriting Contract:
1. `st.title("Review Queue")` then `st.caption("Review every drafted email, tweak the shared template if needed, and approve the ones you're ready to send.")`.
2. `apollo_key = st.secrets.get("APOLLO_API_KEY")` read once at module top (unused this plan but establishes the convention for later plans — do not re-fetch mid-handler, never log it).
3. Path selector (D-11): `st.selectbox("Outreach path", PATH_OPTIONS, index=0)` — deliberately `index=0`, NOT `index=None`/`placeholder=` as Discovery uses, because the queue must always be scoped to exactly one path. Derive `slug = PATH_CONFIG[selected_path]["slug"]`.
4. `st.divider()`.
5. Leave a clearly commented placeholder block where the template editor section will land in plan 04-02 (a single comment line naming D-03 and 04-02 — no widgets, no stub UI).
6. `st.divider()`.
7. Queue section: `st.subheader(f"Drafts Awaiting Review — {selected_path}")` — do NOT pass an `icon=` kwarg to `st.subheader`; the pinned streamlit 1.59.2 does not support it (Phase 3 regression). Render the `:material/fact_check:` intent via the heading text only.
8. Read rows with `get_drafted_by_path(slug)` wrapped in `try/except Exception`; on exception set `rows = None` and render `st.error("Couldn't load the review queue — try reloading the page.", icon=":material/cancel:")`. Never call `st.stop()` on this page.
9. Empty state: when `rows` is an empty list, render `st.info(f"No drafts waiting for review in {selected_path} — head to Contact Discovery to find and personalize more contacts for this path.", icon=":material/inbox:")` and render nothing else. Use `st.info`, never `st.warning`/`st.error` — an empty queue is not a failure.
10. Populated state: render `st.caption(f"{n} draft{'' if n == 1 else 's'} ready for review.")`, then a header row of bold `st.markdown` labels via `st.columns([1, 3, 3, 3, 2])` reading **Select**, **Name**, **Company**, **Title**, **Status**. Then loop the rows; for each row emit the same `st.columns([1, 3, 3, 3, 2])` split. This plan leaves the Select cell and the Status cell empty (a literal empty-string `st.write("")` placeholder with a comment naming 04-04 as the plan that fills them) — the checkbox and badges arrive in 04-04. Fill Name/Company/Title from the row dict with `row.get("name") or "—"`, `row.get("company") or "—"`, `row.get("title") or "—"` so Phase 3 rows with NULL title render an em dash rather than "None".
11. Directly below each row's column strip, render `st.expander(f"View draft — {row.get('name') or '—'} ({row.get('company') or '—'})", icon=":material/mail:", expanded=False)` containing `st.code(full_text, language=None)` where `full_text = f"Subject: {subject}\n\n{body}"` and `(subject, body) = assemble_email(slug, row.get("first_name") or "", row.get("company") or "", row.get("opening_line") or "")`. Wrap the per-row `assemble_email` call in `try/except Exception` and fall back to rendering the stored `opening_line` alone with a one-line caption if assembly raises, so one malformed row never blanks the whole queue. (Plan 04-02 swaps this call for the override-aware version — leave a comment saying so.)

In `app.py`: add `"Review Queue": [st.Page("pages/review_queue_page.py", title="Review Queue")]` to the existing `pages` dict as a new top-level section after `"Discovery"`, matching the existing entry style. Update the trailing comment that says Phase 3+ will append remaining pages so it no longer lists Review Queue as pending. Do NOT add a boot-time secrets gate for the Phase 4 sequence-ID secrets here — those are read and soft-failed on the Review Queue page itself in plan 04-04, so a teammate without sequence IDs can still use Health and Discovery.
  </action>
  <verify>
    <automated>.venv/bin/python3.13 -m py_compile pages/review_queue_page.py app.py && .venv/bin/python3.13 -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python3.13 -m py_compile pages/review_queue_page.py app.py` exits 0.
    - `.venv/bin/python3.13 -m pytest -q` exits 0.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "get_drafted_by_path("` returns at least 1.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "assemble_email("` returns at least 1.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c 'st.selectbox("Outreach path", PATH_OPTIONS, index=0)'` returns 1 (D-11: no placeholder/index=None).
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "st.subheader(.*icon="` returns 0 (streamlit 1.59.2 has no icon kwarg on subheader).
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "st.stop()"` returns 0.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c ":material/inbox:"` returns 1 and the same file contains `st.info(` on that line's statement — empty queue is neutral, not an error.
    - `grep -v '^\s*#' app.py | grep -c "review_queue_page.py"` returns 1.
    - Running `.venv/bin/python3.13 -m streamlit run app.py` and opening "Review Queue" shows the path selector defaulted to "Club Sponsorship", either the drafted rows for that path with working "View draft" expanders or the `:material/inbox:` empty-state message — and no traceback.
  </acceptance_criteria>
  <done>A teammate can open Review Queue from the nav, switch paths, and see every drafted contact for that path with the full email behind a per-row expander, or a neutral empty-state message.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python3.13 -m pytest -q` is green (use `python3.13` explicitly — `.venv/bin/python3` resolves to a stray Python 3.14 with an unpinned streamlit 1.64.0, per 04-RESEARCH.md Pitfall 4).
- `.venv/bin/python3.13 -m streamlit run app.py` boots with three nav entries: System Health, Contact Discovery, Review Queue.
- Switching the path selector re-renders the queue for the newly selected path with no stale rows from the previous path.
</verification>

<success_criteria>
- QUEUE-01: every `status='drafted'` prospect for the selected path is present in the queue read, before any enrollment path exists.
- QUEUE-02: name, company, role (title) and the full assembled email preview are all visible per contact.
- D-10, D-11 honored: one path at a time, table-shaped rows with a per-row "View draft" expander.
- Schema carries `title`, `last_name`, and `template_override` after a single idempotent boot — no further schema work is needed anywhere else in Phase 4.
</success_criteria>

<output>
Create `.planning/phases/04-review-queue-and-sequence-enrollment/04-01-SUMMARY.md` when done.
</output>

