---
phase: 04-review-queue-and-sequence-enrollment
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - personalization/templates.py
  - db/templates_store.py
  - tests/test_templates.py
  - pages/review_queue_page.py
autonomous: true
requirements: [QUEUE-02]

must_haves:
  truths:
    - "D-03: A teammate can edit the shared email subject and body for the currently selected path directly on the Review Queue page, above the list of drafts"
    - "D-06: Saving a template that is missing {first_name}, {company} or {opening_line} is blocked with a plain-language warning, and the teammate's edits are not discarded"
    - "D-04: After a successful save, every draft preview in the queue below immediately reflects the new template text combined with that contact's existing AI opening line"
    - "D-05: An edited template survives an app restart and becomes the new default for that path; an unedited path still falls back to PATH_TEMPLATES"
    - "Editing the template for one path never changes any other path's template"
  artifacts:
    - path: "db/templates_store.py"
      provides: "get_template / save_template_override over the template_override table with PATH_TEMPLATES seed fallback"
      exports: ["get_template", "save_template_override"]
      min_lines: 40
    - path: "personalization/templates.py"
      provides: "validate_template_fields + override-aware assemble_email"
      contains: "def validate_template_fields"
    - path: "tests/test_templates.py"
      provides: "unit coverage for merge-field validation, override round-trip, and override-aware assembly"
      min_lines: 60
  key_links:
    - from: "pages/review_queue_page.py"
      to: "db.templates_store.get_template"
      via: "loads the current subject/body for the selected path into the editor and the previews"
      pattern: "get_template\\("
    - from: "pages/review_queue_page.py"
      to: "personalization.templates.validate_template_fields"
      via: "blocking merge-field check on Save Template click"
      pattern: "validate_template_fields\\("
    - from: "pages/review_queue_page.py"
      to: "db.templates_store.save_template_override"
      via: "persists the edited template after validation passes"
      pattern: "save_template_override\\("
    - from: "db/templates_store.py"
      to: "personalization.templates.PATH_TEMPLATES"
      via: "seed/fallback default when no override row exists"
      pattern: "PATH_TEMPLATES"
---

<objective>
Second vertical slice: a teammate who spots a problem in the shared email copy can fix it once, for the whole path, right where they noticed it — and every draft preview in the queue updates immediately and permanently.

Purpose: This is the folded-in todo from Phase 3's human-verify checkpoint (D-03 through D-06). Without it a bad template means re-running discovery for every contact. It also makes QUEUE-02's "full email preview" honest: what the teammate reads is what the template actually is.
Output: `db/templates_store.py` (new), `validate_template_fields()` + override-aware `assemble_email()` in `personalization/templates.py`, `tests/test_templates.py` (new), and the template editor section on the Review Queue page.
</objective>

## Phase Goal

**As a** non-technical teammate running an outreach campaign, **I want to** review the full queue of drafted emails and approve them in bulk, **so that** the contacts get enrolled into the correct Apollo sequence and the campaign goes live.

This plan delivers the "tweak the shared template before approving" refinement of the review half.

<execution_context>
@/Users/yashpersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yashpersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-CONTEXT.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-RESEARCH.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-PATTERNS.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-UI-SPEC.md
@.planning/phases/04-review-queue-and-sequence-enrollment/04-01-SUMMARY.md

<interfaces>
<!-- Contracts this plan builds on. Do not go exploring — these are the whole surface. -->

db/schema.py (after 04-01):
  template_override table: path TEXT PRIMARY KEY, subject TEXT NOT NULL, body TEXT NOT NULL,
                           updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
  ensure_schema(db_path: str = "db/outreach.db") -> None      # creates it idempotently on boot

db/prospects.py (after 04-01):
  get_drafted_by_path(path_slug: str, db_path="db/outreach.db") -> list[dict]
    # dict keys: id, name, first_name, last_name, company, company_domain, email, title, path, opening_line

personalization/templates.py (existing, this plan extends):
  PATH_TEMPLATES: dict[str, dict]     # {slug: {"subject": str, "body": str}}
  assemble_email(path_slug, first_name, company, opening_line) -> tuple[str, str]
    # today: subject = PATH_TEMPLATES[slug]["subject"].format(company=company)
    #        body    = PATH_TEMPLATES[slug]["body"].format(first_name=..., company=..., opening_line=...)

discovery/logic.py:
  PATH_OPTIONS, PATH_CONFIG   # label -> {"slug": ...}

Merge fields, the complete and only set: {first_name}, {company}, {opening_line}

New contracts this plan creates:
  personalization/templates.py:
    REQUIRED_FIELDS: set[str] = {"first_name", "company", "opening_line"}
    validate_template_fields(subject: str, body: str) -> tuple[bool, str]
    assemble_email(path_slug, first_name, company, opening_line,
                   subject_template: str | None = None,
                   body_template: str | None = None) -> tuple[str, str]
  db/templates_store.py:
    get_template(path_slug: str, db_path: str = "db/outreach.db") -> tuple[str, str]
    save_template_override(path_slug: str, subject: str, body: str, db_path: str = "db/outreach.db") -> None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Merge-field validation, override-aware assembly, and the template_override store</name>
  <files>personalization/templates.py, db/templates_store.py, tests/test_templates.py</files>
  <read_first>
    - personalization/templates.py (whole file — the module docstring's "plain str.format only, no templating engine" rule at lines 1-7, PATH_TEMPLATES at 12-136, and assemble_email at 139-153)
    - db/prospects.py (module docstring at lines 1-8 for the connection-lifecycle + `?`-placeholder rules; insert_enriched at 83-129 for the parameterized-write shape)
    - db/schema.py (the template_override DDL added by 04-01)
    - tests/test_discovery_logic.py (pure-function test style — no fixtures, plain input/output assertions)
    - tests/test_prospects.py (the tmp_db_path fixture usage and ensure_schema-then-assert pattern)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-PATTERNS.md (sections "db/templates_store.py (model/service, new)", "personalization/templates.py (utility, extend)", "tests/test_templates.py (new)")
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-RESEARCH.md (Pattern 4's get_template seed-fallback code and the Code Examples merge-field validator)
  </read_first>
  <behavior>
    - test_validate_template_fields_all_present — a subject containing {company} plus a body containing {first_name}, {company} and {opening_line} returns (True, "OK").
    - test_validate_template_fields_missing_field — a body with {first_name} and {company} but no {opening_line} returns (False, msg) where msg contains "opening_line".
    - test_validate_template_fields_counts_subject_and_body_together — {opening_line} present only in the subject and the rest in the body still returns (True, "OK"); validation is over the combined subject+body text (D-06).
    - test_validate_template_fields_rejects_unknown_field — a body containing {last_name} returns (False, msg) where msg contains "last_name"; an unknown placeholder would raise KeyError at assembly time and must never reach a real contact.
    - test_assemble_email_uses_override_templates — assemble_email("club_sponsorship", "Jamie", "Acme", "Opener.", subject_template="Hi from {company}", body_template="Hey {first_name}! {opening_line} — about {company}.") returns exactly ("Hi from Acme", "Hey Jamie! Opener. — about Acme.") and does not consult PATH_TEMPLATES.
    - test_assemble_email_default_unchanged — calling assemble_email with only the original four positional args returns the same (subject, body) it returned before this change for a known slug (assert the subject equals PATH_TEMPLATES["club_sponsorship"]["subject"].format(first_name="Jamie", company="Acme", opening_line="Opener.")).
    - test_get_template_falls_back_to_path_templates — on a fresh ensure_schema'd temp db with no override rows, get_template("productthon", db_path=tmp) returns exactly PATH_TEMPLATES["productthon"]["subject"] and ["body"], and afterwards `SELECT COUNT(*) FROM template_override` is still 0 (read must not write a seed row).
    - test_save_template_override_roundtrip_and_upsert — save_template_override("club_sponsorship", "S1", "B1", db_path=tmp) then get_template returns ("S1","B1"); saving again with ("S2","B2") returns ("S2","B2") and `SELECT COUNT(*) FROM template_override WHERE path='club_sponsorship'` is 1 (upsert, not duplicate rows).
    - test_save_template_override_is_path_scoped — saving an override for club_sponsorship leaves get_template("client_sourcing") on the PATH_TEMPLATES default.
  </behavior>
  <action>
Write `tests/test_templates.py` FIRST with the behaviors above and confirm it fails. Give the file a module docstring stating the deferred-import-inside-the-test-body convention used by every other test file in this repo. Use the existing `tmp_db_path` fixture from `tests/conftest.py` for the store tests; the pure-function tests take no fixtures.

In `personalization/templates.py`:
1. Add `import re` and `from __future__ import annotations` stays at the top as-is. Define `REQUIRED_FIELDS = {"first_name", "company", "opening_line"}` as a module constant directly above the new validator.
2. Add `validate_template_fields(subject: str, body: str) -> tuple[bool, str]` immediately below `assemble_email`. Scan the combined `f"{subject}\n{body}"` text with `re.findall(r"\{(\w+)\}", ...)`. If `REQUIRED_FIELDS - present` is non-empty, return `(False, f"Missing merge field(s): {', '.join(sorted(missing))}. Add them back before saving — without them, contacts would receive a broken or generic email.")` (exact copy from 04-UI-SPEC.md's Copywriting Contract). If `present - REQUIRED_FIELDS` is non-empty, return `(False, f"Unknown merge field(s): {', '.join(sorted(unknown))}. Only {{first_name}}, {{company}} and {{opening_line}} are available.")` — this second branch is a defensive extension of D-06: an unknown placeholder makes `str.format` raise `KeyError` during assembly, which would break every preview on the page. Otherwise return `(True, "OK")`. Pure function — no I/O, no logging.
3. Extend `assemble_email` with two keyword-only-style optional parameters appended after the existing four: `subject_template: str | None = None, body_template: str | None = None`. When either is `None`, fall back to `PATH_TEMPLATES[path_slug]["subject"]` / `["body"]` respectively (preserving the current behavior and every existing call site in `pages/discovery_page.py` and `pages/review_queue_page.py`). Format the SUBJECT with all three merge fields (`first_name=..., company=..., opening_line=...`), not just `company` as today — a teammate may legitimately put `{first_name}` in a subject, and the current single-field `.format` would raise KeyError. Update the docstring to record D-04/D-05: the override text, when supplied, wins over PATH_TEMPLATES, and this module still never touches SQLite.

Create `db/templates_store.py`:
4. Module docstring mirroring `db/prospects.py`'s: state that this is D-05's per-path template persistence, that it follows the fresh-`sqlite3.connect()`-per-call + `try/finally` lifecycle, and that all SQL uses `?` placeholders exclusively — teammate-edited subject/body text is free-text user input and must never be interpolated into a SQL string.
5. `get_template(path_slug, db_path="db/outreach.db") -> tuple[str, str]`: `SELECT subject, body FROM template_override WHERE path = ?`; if a row exists return it, otherwise return `(PATH_TEMPLATES[path_slug]["subject"], PATH_TEMPLATES[path_slug]["body"])`. Lazy fallback only — this function must NOT write a seed row on read (04-RESEARCH.md Assumption A4), so merely viewing the queue never creates a permanent override.
6. `save_template_override(path_slug, subject, body, db_path="db/outreach.db") -> None`: single `INSERT INTO template_override (path, subject, body, updated_at) VALUES (?, ?, ?, CURRENT_TIMESTAMP) ON CONFLICT(path) DO UPDATE SET subject = excluded.subject, body = excluded.body, updated_at = CURRENT_TIMESTAMP`. Docstring must state that the caller is responsible for running `validate_template_fields` first and that this function deliberately does not re-validate. This upsert is the one new SQL shape in the codebase — keep it inside this single function, do not generalize a helper.
  </action>
  <verify>
    <automated>.venv/bin/python3.13 -m pytest tests/test_templates.py tests/test_personalization.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python3.13 -m pytest tests/test_templates.py tests/test_personalization.py -q` exits 0 with all nine behaviors above passing.
    - `.venv/bin/python3.13 -m pytest -q` exits 0 — `pages/discovery_page.py`'s existing four-argument `assemble_email` call still works unchanged.
    - `grep -v '^\s*#' personalization/templates.py | grep -c "def validate_template_fields"` returns 1.
    - `grep -v '^\s*#' personalization/templates.py | grep -c "sqlite3"` returns 0 — this module still performs no I/O.
    - `grep -v '^\s*#' db/templates_store.py | grep -c "ON CONFLICT(path) DO UPDATE"` returns 1.
    - `grep -v '^\s*#' db/templates_store.py | grep -cE "(format\(|f\"SELECT|f\"INSERT|%s)"` returns 0 — no string interpolation into SQL.
    - `.venv/bin/python3.13 -c "from personalization.templates import validate_template_fields as v; print(v('{company}', 'Hi {first_name} {opening_line}')); print(v('x', 'Hi {first_name} {company}'))"` prints `(True, 'OK')` then a `False` tuple whose message contains `opening_line`.
  </acceptance_criteria>
  <done>Template overrides persist per path with a PATH_TEMPLATES fallback, assembly can run against override text, and merge-field validation rejects both missing and unknown placeholders — all unit tested.</done>
</task>

<task type="auto">
  <name>Task 2: Template editor section on the Review Queue page</name>
  <files>pages/review_queue_page.py</files>
  <read_first>
    - pages/review_queue_page.py (whole file as created by 04-01 — the commented placeholder block between the two dividers is where this section lands, and the per-row `assemble_email` call in the expander block must switch to the override-aware form)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-UI-SPEC.md ("Template Editor Contract" table and the Copywriting Contract rows for the editor — every label, caption, warning and success string is locked there)
    - pages/discovery_page.py (lines 88-140 — the button-handler + session-state-result convention; lines 266-294 for the banner render convention)
    - db/templates_store.py and personalization/templates.py (the exact signatures created in Task 1)
    - .planning/phases/04-review-queue-and-sequence-enrollment/04-CONTEXT.md (D-03, D-04, D-05, D-06)
  </read_first>
  <action>
Replace the commented placeholder block in `pages/review_queue_page.py` with the template editor section, placed above the queue and below the path selector (D-03). Import `get_template` and `save_template_override` from `db.templates_store` and `validate_template_fields` from `personalization.templates`.

Section contents, using 04-UI-SPEC.md's exact copy:
1. `st.subheader(f"Email Template — {selected_path}")` — no `icon=` kwarg (streamlit 1.59.2 does not support it on subheader).
2. Load the current text once per run: `current_subject, current_body = get_template(slug)`, wrapped in `try/except Exception` that degrades to `PATH_TEMPLATES[slug]` values plus an `st.warning` rather than raising.
3. `st.text_input("Email subject", value=current_subject, key=f"tpl_subject_{slug}")` and `st.text_area("Email body", value=current_body, height=240, key=f"tpl_body_{slug}")`. The per-slug `key` suffix is required so switching paths re-renders the editor with that path's text instead of carrying the previous path's edits across (04-UI-SPEC.md's "Path switch" row: unsaved edits are discarded on path change).
4. `st.caption("Keep {first_name}, {company}, and {opening_line} somewhere in the subject or body — these get replaced with each contact's real details.")`.
5. `st.button("Save Template", type="primary", icon=":material/save:")`. On click: run `validate_template_fields(edited_subject, edited_body)` BEFORE any write. On failure, render `st.warning(message, icon=":material/error:")` and do not call `save_template_override` — the text area keeps the teammate's edits because the widget state is untouched. On success, call `save_template_override(slug, edited_subject, edited_body)` inside `try/except Exception` (an exception degrades to `st.error("Couldn't save the template — try again.", icon=":material/cancel:")`), then render `st.success("Template saved — every draft below now uses the updated text.", icon=":material/check_circle:")` and call `st.rerun()` so the queue below re-reads the saved override.
6. Store the save outcome in a `st.session_state.template_result` dict initialized to `None` alongside the other session-state keys (shape `{"error": str | None, "message": str}`), following `pages/discovery_page.py`'s session-state-result convention, so the success banner survives the `st.rerun()`.

Then update the queue rendering block below: change the per-row assembly call to pass the loaded override, i.e. `assemble_email(slug, row.get("first_name") or "", row.get("company") or "", row.get("opening_line") or "", subject_template=current_subject, body_template=current_body)`. This is what makes D-04 true — after a save, every visible preview reflects the new template combined with that contact's existing AI opening line, with no stale text and no page reload needed. Remove the 04-01 comment that said this call would be swapped later.
  </action>
  <verify>
    <automated>.venv/bin/python3.13 -m py_compile pages/review_queue_page.py && .venv/bin/python3.13 -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python3.13 -m py_compile pages/review_queue_page.py` exits 0 and `.venv/bin/python3.13 -m pytest -q` exits 0.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "validate_template_fields("` returns at least 1.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "save_template_override("` returns 1.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "subject_template=current_subject"` returns 1 — previews assemble from the override, not PATH_TEMPLATES.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "height=240"` returns 1.
    - `grep -v '^\s*#' pages/review_queue_page.py | grep -c "Keep {first_name}, {company}, and {opening_line}"` returns 1 (exact UI-SPEC helper caption).
    - In `.venv/bin/python3.13 -m streamlit run app.py`: deleting `{opening_line}` from the body and clicking "Save Template" shows the yellow missing-merge-field warning, leaves the edited text in place, and leaves every draft preview unchanged.
    - Restoring `{opening_line}`, editing one visible sentence, and clicking "Save Template" shows the green "Template saved" line and every "View draft" expander below immediately contains the edited sentence; restarting the app with `.venv/bin/python3.13 -m streamlit run app.py` still shows the edited text (D-05).
  </acceptance_criteria>
  <done>A teammate can fix the shared copy for a path in one place, is blocked from saving a broken template, and sees every queued draft update immediately and permanently.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python3.13 -m pytest -q` green (always `python3.13` — the bare `python3` symlink in this venv resolves to an unpinned streamlit 1.64.0, 04-RESEARCH.md Pitfall 4).
- Switching paths in the selector loads that path's own template into the editor and discards unsaved edits from the previous path.
- An unedited path still shows its `PATH_TEMPLATES` default text and has no row in `template_override`.
</verification>

<success_criteria>
- D-03: editor lives on the Review Queue page above the drafts, never on Discovery's read-only Email Drafts section.
- D-04: saving re-assembles every visible draft immediately — no stale previews.
- D-05: overrides persist across restarts; PATH_TEMPLATES remains the seed/fallback.
- D-06: all three merge fields are enforced before any write.
</success_criteria>

<output>
Create `.planning/phases/04-review-queue-and-sequence-enrollment/04-02-SUMMARY.md` when done.
</output>
