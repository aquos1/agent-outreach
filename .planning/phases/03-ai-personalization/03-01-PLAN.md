---
phase: 03-ai-personalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - .streamlit/secrets.toml.example
  - app.py
  - tests/conftest.py
  - tests/test_personalization.py
  - tests/test_prospects.py
  - tests/test_schema.py
autonomous: true
requirements: [PERS-01]

user_setup:
  - service: anthropic
    why: "Claude Haiku generates the personalized opening line for every contact (PERS-01)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com -> Settings -> API Keys -> Create Key; paste into .streamlit/secrets.toml locally and the Streamlit Community Cloud secrets console for production"

must_haves:
  truths:
    - "The app refuses to boot with a plain-language message (not a traceback) when ANTHROPIC_API_KEY is missing"
    - "The full RED test suite for Phase 3 collects successfully and fails only on missing implementation, never on collection errors"
  artifacts:
    - path: "requirements.txt"
      provides: "anthropic dependency pin"
      contains: "anthropic=="
    - path: ".streamlit/secrets.toml.example"
      provides: "ANTHROPIC_API_KEY placeholder for local + cloud setup"
      contains: "ANTHROPIC_API_KEY"
    - path: "app.py"
      provides: "boot-time ANTHROPIC_API_KEY presence gate"
      contains: "ANTHROPIC_API_KEY"
    - path: "tests/test_personalization.py"
      provides: "RED unit tests for generator + templates"
      min_lines: 80
    - path: "tests/conftest.py"
      provides: "mock_anthropic_message factory fixture"
      contains: "mock_anthropic_message"
  key_links:
    - from: "app.py"
      to: "st.secrets ANTHROPIC_API_KEY"
      via: "boot-time presence gate mirroring the APOLLO_API_KEY gate"
      pattern: "st\\.secrets\\.get\\(\"ANTHROPIC_API_KEY\"\\)"
    - from: "tests/test_personalization.py"
      to: "personalization.generator"
      via: "deferred in-test-body import"
      pattern: "from personalization\\.generator import"
---

<objective>
Install and gate the Anthropic dependency, then land the complete Wave 0 RED test harness for Phase 3 so every downstream implementation plan has an automated, sub-10-second feedback signal.

Purpose: `03-VALIDATION.md` requires every Phase 3 task to have an `<automated>` verify command; all seven of those commands point at test files that do not exist yet. Nothing in this phase can be verified until this plan lands.
Output: `anthropic==1.6.0` pinned and installed, `ANTHROPIC_API_KEY` wired into the secrets example and the `app.py` boot gate, `mock_anthropic_message` fixture, and RED tests in `tests/test_personalization.py`, `tests/test_prospects.py`, `tests/test_schema.py`.
</objective>

## Phase Goal

**As a** teammate running an outreach campaign, **I want to** see a complete, personalized email draft for every contact I just enriched, **so that** I can trust exactly what will be sent before anything goes out.

<execution_context>
@/Users/yashpersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yashpersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-personalization/03-CONTEXT.md
@.planning/phases/03-ai-personalization/03-RESEARCH.md
@.planning/phases/03-ai-personalization/03-PATTERNS.md
@.planning/phases/03-ai-personalization/03-VALIDATION.md

<interfaces>
<!-- Contracts the RED tests in this plan must assert against. Plans 03-02 and 03-03 implement them. -->
<!-- Do NOT explore the codebase to discover these — they are defined here. -->

personalization/generator.py (created by plan 03-02):
  MODEL: str  # "claude-haiku-4-5-20251001"
  SYSTEM_PROMPT: str
  FALLBACK_LINE: str  # contains the "{company}" merge field
  FALLBACK_BANNER_THRESHOLD: float  # 0.5
  make_client(api_key: str) -> anthropic.Anthropic
  _is_sparse_title(title: str | None) -> bool
  generate_opening_line(client, title: str, company: str) -> tuple[str | None, str]
  build_opening_line(client, title: str | None, company: str) -> tuple[str, str]   # -> (opening_line, source) where source is "ai" | "fallback"
  should_warn_fallback_rate(fallback_count: int, total: int) -> bool

personalization/templates.py (created by plan 03-02):
  PATH_TEMPLATES: dict[str, dict[str, str]]   # keys: "club_sponsorship", "productthon", "client_sourcing"; each value has "subject" and "body"
  assemble_email(path_slug: str, first_name: str, company: str, opening_line: str) -> tuple[str, str]   # -> (subject, body)

db/prospects.py (extended by plan 03-03):
  update_draft(prospect_id: str | int, opening_line: str, source: str, first_name: str | None = None, db_path: str = "db/outreach.db") -> None

db/schema.py (extended by plan 03-03):
  ensure_schema(db_path: str = "db/outreach.db") -> None   # now additionally migrates opening_line, first_name, draft_source columns onto an existing prospect table
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin and install the anthropic SDK, add ANTHROPIC_API_KEY to the secrets example and the app.py boot gate</name>
  <files>requirements.txt, .streamlit/secrets.toml.example, app.py</files>
  <read_first>
    - requirements.txt (current pins: streamlit==1.59.2, requests==2.34.2, dnspython==2.8.0 — exact `==` pin convention must be matched)
    - .streamlit/secrets.toml.example (current two-key placeholder style)
    - app.py (lines 21-46: the existing `ensure_schema()` call, the `APOLLO_API_KEY` presence gate, and the `st.navigation` registration — the new gate goes between the existing gate and the navigation block)
    - .planning/phases/03-ai-personalization/03-PATTERNS.md (sections "app.py (config, extend)", "requirements.txt (extend)", ".streamlit/secrets.toml.example (extend)")
  </read_first>
  <action>
Add `anthropic==1.6.0` as a fourth line of `requirements.txt`, matching the existing exact-`==`-pin convention (not `>=`). This satisfies CLAUDE.md's `anthropic>=0.117.0` constraint and is the version verified in 03-RESEARCH.md's Package Legitimacy Audit (disposition: Approved, slopcheck `[OK]` — no legitimacy checkpoint required). Install it into the active environment with `pip install anthropic==1.6.0`.

Add `ANTHROPIC_API_KEY = "your-anthropic-api-key"` as a third line of `.streamlit/secrets.toml.example`, matching the existing placeholder-string style of `APOLLO_API_KEY` and `SENDING_DOMAIN`.

In `app.py`, read `anthropic_key = st.secrets.get("ANTHROPIC_API_KEY")` alongside the existing `apollo_key`/`sending_domain` reads, and add a second presence gate immediately after the existing `if not apollo_key:` block and before the `pages = {...}` navigation dict. The new gate uses the same `st.error(...)` + `st.stop()` fail-closed shape with the message text: `ANTHROPIC_API_KEY is not set. Add it to .streamlit/secrets.toml (local) or the Community Cloud secrets console (production).` Update `app.py`'s module docstring step 2 to say the secrets presence gate now covers both `APOLLO_API_KEY` and `ANTHROPIC_API_KEY`. Never log, print, or interpolate either key value into any message (T-04-02).

Do not add a Personalization page to the `pages` dict — D-02 locks draft visibility to the existing Discovery page; no new page exists in this phase.
  </action>
  <verify>
    <automated>python -c "import anthropic, importlib.metadata as m; print(m.version('anthropic'))" && grep -c '^anthropic==1.6.0$' requirements.txt && grep -c 'ANTHROPIC_API_KEY' .streamlit/secrets.toml.example && python -m py_compile app.py && test "$(grep -v '^\s*#' app.py | grep -c 'ANTHROPIC_API_KEY')" -ge 2 && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import anthropic"` exits 0 and reports installed version `1.6.0`
    - `requirements.txt` contains a line exactly equal to `anthropic==1.6.0`
    - `.streamlit/secrets.toml.example` contains a line starting with `ANTHROPIC_API_KEY =`
    - `grep -v '^\s*#' app.py | grep -c 'ANTHROPIC_API_KEY'` returns at least 2 (the `st.secrets.get` read and the `st.error` message), proving the reference is in executable code and not only in a comment
    - `grep -v '^\s*#' app.py | grep -c 'st.stop()'` returns 2 (the pre-existing Apollo gate plus the new Anthropic gate)
    - `python -m py_compile app.py` exits 0
    - No new entry is added to the `pages` dict in `app.py`
  </acceptance_criteria>
  <done>The anthropic SDK is installed and pinned, the secrets example documents ANTHROPIC_API_KEY, and app.py fails closed with a plain-language banner when the key is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Add the mock_anthropic_message fixture and the full Wave 0 RED test suite</name>
  <files>tests/conftest.py, tests/test_personalization.py, tests/test_prospects.py, tests/test_schema.py</files>
  <read_first>
    - tests/conftest.py (the `_FakeResponse` class + `mock_requests_response` factory fixture at lines 23-41 — the exact shape the new fixture must mirror; also `tmp_db_path` at lines 17-20)
    - tests/test_apollo_client.py (module docstring's deferred-import rationale, and `test_search_people_error_codes` / `test_search_people_never_raises_on_network` — the monkeypatched-client error-table pattern)
    - tests/test_prospects.py (`test_insert_enriched_writes_enriched_status` — the ensure_schema -> seed -> act -> raw-SQL-assert shape)
    - tests/test_schema.py (`test_ensure_schema_idempotent` — the double-call idempotency shape)
    - db/schema.py (the `DDL` prospect CREATE TABLE statement — the migration test must build a pre-migration table that omits opening_line/first_name/draft_source)
    - .planning/phases/03-ai-personalization/03-VALIDATION.md (Per-Task Verification Map — the exact test function names are contractual)
    - .planning/phases/03-ai-personalization/03-CONTEXT.md (`<specifics>` section — the verbatim per-path template text the assembly test asserts against)
    - .planning/phases/03-ai-personalization/03-PATTERNS.md (sections "tests/test_personalization.py", "tests/test_prospects.py (extend)", "tests/test_schema.py (extend)", "tests/conftest.py (extend)")
  </read_first>
  <action>
These are RED tests — the modules under test do not exist yet. Follow this repo's established convention of performing every `from personalization.generator import ...` / `from personalization.templates import ...` / `from db.prospects import ...` import **inside the test function body**, never at module top, so `pytest --collect-only` succeeds before plans 03-02 and 03-03 land.

In `tests/conftest.py`, add a `mock_anthropic_message` factory fixture mirroring the existing `mock_requests_response` shape: a minimal stand-in object whose `.content` attribute is a list of `types.SimpleNamespace(type="text", text=<given text>)` blocks, so tests can monkeypatch a fake `client.messages.create` without a real API key. Signature: `mock_anthropic_message(text: str)`. Do not import the real `anthropic` response classes — this is a stand-in, matching `_FakeResponse`'s philosophy.

Create `tests/test_personalization.py` with a module docstring stating these are RED tests for `personalization/generator.py` and `personalization/templates.py` (not yet implemented) and that imports are deferred into test bodies. Implement exactly these six test functions:

1. `test_prompt_excludes_industry_and_seniority` — build a fake client object whose `messages.create(**kwargs)` records `kwargs` and returns `mock_anthropic_message("A grounded opener.")`. Call `generate_opening_line(fake_client, title="Head of Partnerships", company="Acme Corp")`. Assert the recorded `system` string plus the recorded `messages[0]["content"]` string, lowercased and concatenated, contain `"head of partnerships"` and `"acme corp"` but contain neither `"industry"` nor `"seniority"` (D-06 grounding). Assert `kwargs["model"] == "claude-haiku-4-5-20251001"`.
2. `test_sparse_title_triggers_fallback` — build a fake client whose `messages.create` raises `AssertionError("API must not be called for a sparse title")` if invoked. For each of `None`, `""`, `"   "`, `"N/A"`, `"unknown"`, `"-"`, call `build_opening_line(fake_client, title, company="Acme Corp")` and assert the returned source is `"fallback"`, the returned line is a non-empty string, and the line contains `"Acme Corp"` (the fallback references company only, never title — D-07).
3. `test_api_failure_triggers_fallback` — parametrize over `anthropic.APIConnectionError`, `anthropic.RateLimitError`, and `anthropic.APITimeoutError`. Construct each with the arguments the SDK requires (`APIConnectionError(request=httpx.Request("POST", "https://api.anthropic.com/v1/messages"))`; `APITimeoutError(request=...)`; for `RateLimitError`, pass `message`, `response=httpx.Response(429, request=...)`, and `body=None`) — `httpx` is an `anthropic` dependency and is importable. Monkeypatch the fake client's `messages.create` to raise, then assert `build_opening_line(...)` returns source `"fallback"` with a non-empty line and that no exception escapes (D-14).
4. `test_generate_opening_line_strips_quotes_and_whitespace` — fake client returns `mock_anthropic_message('  "Congrats on the new role at Acme Corp."  ')`. Assert `generate_opening_line(...)` returns a line that does not start or end with `"` and has no leading/trailing whitespace (Pitfall 4). Also assert that a response whose text is only whitespace returns `(None, <non-empty message>)`.
5. `test_assemble_email_per_path` — for each slug in `"club_sponsorship"`, `"productthon"`, `"client_sourcing"`, call `assemble_email(slug, first_name="Jamie", company="Acme Corp", opening_line="OPENER_SENTINEL")` and assert: the returned tuple is `(subject, body)` of two non-empty strings; `"OPENER_SENTINEL"` appears in `body`; the body line immediately following the greeting line is the opening line (split body on `"\n\n"` — element 0 is the greeting, element 1 is exactly `"OPENER_SENTINEL"`, per D-09); `"Jamie"` appears in the body; `"Acme Corp"` appears in the body; neither `subject` nor `body` contains `"{"` or `"["` + `"First Name"` or `"[Company]"` (no unrendered merge fields); and `"Best regards,"` and `"Yash Kulkarni"` appear in every body. Additionally assert `"[SPONSORSHIP_LINK]"` is present in the `club_sponsorship` and `productthon` bodies and absent from `client_sourcing` (D-11), and that the `client_sourcing` subject equals `"Partner with Product Space UW on Your Next Product Initiative"` (D-12).
6. `test_fallback_fraction_threshold` — assert `should_warn_fallback_rate(6, 10) is True`, `should_warn_fallback_rate(5, 10) is False`, `should_warn_fallback_rate(0, 10) is False`, `should_warn_fallback_rate(10, 10) is True`, and `should_warn_fallback_rate(0, 0) is False` (no division by zero on an empty batch — D-16 with the planner-set 0.5 strict-majority threshold).

Extend `tests/test_prospects.py` with `test_update_draft_writes_status_drafted(tmp_db_path)`: call `ensure_schema(tmp_db_path)`, seed one row via `insert_enriched` with a match dict carrying `id="apollo-1"`, then call `update_draft("apollo-1", "A grounded opener.", "ai", first_name="Jamie", db_path=tmp_db_path)`. Reconnect with raw `sqlite3` and assert via direct SELECT that `opening_line`, `draft_source`, `first_name`, and `status` are `"A grounded opener."`, `"ai"`, `"Jamie"`, and `"drafted"` respectively. Add a second assertion that a row for an unrelated `apollo_person_id` is untouched.

Extend `tests/test_schema.py` with `test_column_migration_idempotent(tmp_db_path)`: connect raw `sqlite3` to `tmp_db_path` and `CREATE TABLE prospect` with only the pre-Phase-3 columns (id, name, company, company_domain, apollo_person_id, apollo_contact_id, email, path, status, sequence_id, created_at, updated_at — copy the shape from `db/schema.py`'s `DDL`, omitting the three new columns), insert one row, close, then call `ensure_schema(tmp_db_path)` twice. Assert neither call raises, that `PRAGMA table_info(prospect)` now lists `opening_line`, `first_name`, and `draft_source`, and that the pre-existing row still exists (row count is 1) — this directly guards RESEARCH.md Pitfall 1.
  </action>
  <verify>
    <automated>pytest tests/test_personalization.py tests/test_prospects.py tests/test_schema.py --collect-only -q && test "$(pytest tests/test_personalization.py --collect-only -q 2>/dev/null | grep -c '::test_')" -eq 6</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_personalization.py tests/test_prospects.py tests/test_schema.py --collect-only -q` exits 0 (no collection errors — proving imports are deferred into test bodies)
    - `pytest tests/test_personalization.py --collect-only -q` lists exactly these six test IDs: `test_prompt_excludes_industry_and_seniority`, `test_sparse_title_triggers_fallback`, `test_api_failure_triggers_fallback`, `test_generate_opening_line_strips_quotes_and_whitespace`, `test_assemble_email_per_path`, `test_fallback_fraction_threshold`
    - `pytest tests/test_personalization.py -q` reports failures (RED) whose cause is `ModuleNotFoundError: No module named 'personalization'` — not a syntax, fixture, or collection error
    - `pytest tests/test_prospects.py::test_update_draft_writes_status_drafted -q` fails with `ImportError`/`AttributeError` on `update_draft`, not a collection error
    - `pytest tests/test_schema.py::test_column_migration_idempotent -q` fails on a missing column assertion, not a collection error
    - `grep -c 'mock_anthropic_message' tests/conftest.py` returns at least 2
    - Every pre-existing Phase 1/2 test still passes: `pytest tests/test_apollo_client.py tests/test_discovery_logic.py tests/test_dns_checks.py -q` exits 0
    - No test in `tests/test_personalization.py` makes a live network call (no real `anthropic.Anthropic()` construction with a real key, no `requests`/`httpx` client calls)
  </acceptance_criteria>
  <done>All seven automated commands in 03-VALIDATION.md's Per-Task Verification Map resolve to real, collectible test functions that fail only because the implementation does not exist yet.</done>
</task>

</tasks>

<verification>
- `pytest -q` collects the full suite with zero collection errors
- Phase 1 and Phase 2 tests remain green (no regressions from the conftest change)
- New Phase 3 tests are RED for the expected reason (missing implementation), which is the correct Wave 0 state
- `python -m py_compile app.py` exits 0
</verification>

<success_criteria>
- `anthropic==1.6.0` is pinned in requirements.txt and importable
- `ANTHROPIC_API_KEY` is documented in `.streamlit/secrets.toml.example` and gated at boot in `app.py`
- Six named tests exist in `tests/test_personalization.py`, one in `tests/test_prospects.py`, one in `tests/test_schema.py`, all collectible and all RED
- `mock_anthropic_message` fixture is available to every test module
</success_criteria>

<output>
Create `.planning/phases/03-ai-personalization/03-01-SUMMARY.md` when done.
Flag in the SUMMARY's user-setup section: the teammate must add a real `ANTHROPIC_API_KEY` to `.streamlit/secrets.toml` before the app will boot.
</output>
