---
phase: 03-ai-personalization
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - pages/discovery_page.py
autonomous: false
requirements: [PERS-01]

must_haves:
  truths:
    - "Clicking Enrich & Continue automatically generates a draft for every enriched contact — there is no second button to press"
    - "A progress indicator reports how many contacts have been processed while generation runs"
    - "Every contact row has a collapsed 'View draft' expander showing the full assembled subject and body as one continuous block"
    - "When more than half the batch fell back to the generic opener, a non-blocking warning banner appears; below that threshold nothing is surfaced"
    - "The page never shows a traceback and never hard-stops, even if the Anthropic API is entirely unavailable"
  artifacts:
    - path: "pages/discovery_page.py"
      provides: "chained draft generation, progress bar, per-contact draft expanders, D-16 banner"
      contains: "build_opening_line"
      min_lines: 250
  key_links:
    - from: "pages/discovery_page.py"
      to: "personalization.generator.build_opening_line"
      via: "per-contact call inside the Enrich & Continue handler"
      pattern: "build_opening_line\\("
    - from: "pages/discovery_page.py"
      to: "personalization.templates.assemble_email"
      via: "per-contact subject/body assembly"
      pattern: "assemble_email\\("
    - from: "pages/discovery_page.py"
      to: "db.prospects.update_draft"
      via: "per-contact persistence advancing status to drafted"
      pattern: "update_draft\\("
    - from: "pages/discovery_page.py"
      to: "st.session_state.draft_result"
      via: "session-state result dict read back for rendering"
      pattern: "st\\.session_state\\.draft_result"
---

<objective>
Wire draft generation into the Discovery page's existing "Enrich & Continue" handler and render the per-contact draft previews, completing the Phase 3 vertical slice.

Purpose: This is the task that turns Phase 3 into something a teammate can actually see and use — after this plan, enriching contacts produces visible, reviewable email drafts with no extra clicks.
Output: `pages/discovery_page.py` chains generation after `insert_enriched()`, shows progress, renders an "Email Drafts" section of expanders, and conditionally warns on a high fallback rate.
</objective>

## Phase Goal

**As a** teammate running an outreach campaign, **I want to** see a complete, personalized email draft for every contact I just enriched, **so that** I can trust exactly what will be sent before anything goes out.

<execution_context>
@/Users/yashpersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yashpersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-ai-personalization/03-CONTEXT.md
@.planning/phases/03-ai-personalization/03-UI-SPEC.md
@.planning/phases/03-ai-personalization/03-RESEARCH.md
@.planning/phases/03-ai-personalization/03-PATTERNS.md
@.planning/phases/03-ai-personalization/03-02-SUMMARY.md
@.planning/phases/03-ai-personalization/03-03-SUMMARY.md

<interfaces>
<!-- Everything this page consumes already exists after plans 03-02 and 03-03. No exploration needed. -->

personalization/generator.py:
  make_client(api_key: str) -> anthropic.Anthropic         # already configured with max_retries=1, timeout=20.0
  build_opening_line(client, title: str | None, company: str) -> tuple[str, str]   # (opening_line, "ai" | "fallback"); never raises
  should_warn_fallback_rate(fallback_count: int, total: int) -> bool

personalization/templates.py:
  assemble_email(path_slug: str, first_name: str, company: str, opening_line: str) -> tuple[str, str]   # (subject, body)

db/prospects.py:
  update_draft(prospect_id, opening_line: str, source: str, first_name: str | None = None, db_path: str = "db/outreach.db") -> None

Already imported in this page:
  discovery.logic: PATH_CONFIG, PATH_OPTIONS, CONTACT_CAP, build_search_filters, cost_estimate, filter_has_email
  apollo.client: search_people, enrich_candidates
  db.prospects: dedup_filter, insert_enriched

Apollo match dict fields used here (all read defensively with .get()):
  match["id"]            -> apollo_person_id, the update_draft WHERE key
  match["name"]          -> display name in the table and expander label
  match["first_name"]    -> the {first_name} merge field (NEVER derive it by splitting match["name"])
  match["title"]         -> the grounding field passed to build_opening_line
  match["organization"]["name"] -> the {company} merge field (organization may be absent; use (match.get("organization") or {}))
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chain draft generation into Enrich & Continue and render the Email Drafts section</name>
  <files>pages/discovery_page.py</files>
  <read_first>
    - pages/discovery_page.py (whole file — module docstring rules at lines 14-24, the session-state init block at lines 50-53, the "Enrich & Continue" handler at lines 145-174, and the results-render block at lines 176-200 where the new section attaches)
    - pages/health_page.py (the SPF/DMARC soft-block section — the `st.warning(..., icon=":material/error:")` non-blocking banner convention)
    - .planning/phases/03-ai-personalization/03-UI-SPEC.md (Interaction Contract sections 1-5 and the Copywriting Contract — every string and widget choice in this task is locked there)
    - .planning/phases/03-ai-personalization/03-RESEARCH.md (Code Examples: "Full Anthropic call + fallback wiring inside the Discovery page's existing handler"; Pitfall 2 on first_name)
    - .planning/phases/03-ai-personalization/03-PATTERNS.md (section "pages/discovery_page.py (controller, extend)")
    - personalization/generator.py and personalization/templates.py (the functions being called)
  </read_first>
  <action>
Import `build_opening_line`, `make_client`, and `should_warn_fallback_rate` from `personalization.generator`, `assemble_email` from `personalization.templates`, and add `update_draft` to the existing `db.prospects` import. Read `anthropic_key = st.secrets.get("ANTHROPIC_API_KEY")` once at module top, directly beside the existing `apollo_key` read — never re-fetch it mid-handler and never interpolate it into any message or log line.

Add `draft_result` to the session-state init block alongside `find_result` and `enrich_result`, initialized to `None`. Also reset `st.session_state.draft_result = None` wherever the existing code resets `enrich_result` (the "Find Contacts" handler), so a new search never shows stale drafts.

Inside the existing "Enrich & Continue" handler, in the `else:` branch immediately after `insert_enriched(matches, path=slug)` succeeds, chain draft generation — D-01 makes this automatic, so do NOT add a second button, a toggle, or a confirmation step. The whole generation block is wrapped in `try/except Exception` following this page's established convention: any unexpected escape writes an error into `st.session_state.draft_result` rather than surfacing a traceback, and never calls `st.stop()` (this page's docstring rule: inputs and buttons stay usable).

The generation block:
1. Build one client with `make_client(anthropic_key)` before the loop — one client for the whole batch, not one per contact.
2. Create a progress indicator held in an `st.empty()` placeholder so it can be cleared afterwards. Initial text is exactly `Personalizing drafts... 0/{total}` where `{total}` is `len(matches)`.
3. Loop over `matches` with an index. For each match read `title = match.get("title")`, `company = (match.get("organization") or {}).get("name") or ""`, and `first_name = match.get("first_name") or ""` — defensive `.get()` on every Apollo-derived field, and never derive `first_name` by splitting `match.get("name")`.
4. Call `build_opening_line(client, title, company)` to get `(opening_line, source)`. All fallback decision-making — sparse title and API failure alike — lives inside that function; do not re-implement either branch here. Increment a `fallback_count` when `source == "fallback"`.
5. Call `assemble_email(slug, first_name, company, opening_line)` to get `(subject, body)`, using the same `slug` already computed from `PATH_CONFIG[selected_path]["slug"]`.
6. Call `update_draft(match.get("id"), opening_line, source, first_name=first_name)` to persist and advance the row to `status='drafted'` (D-04).
7. Append a dict with `name`, `company`, `subject`, `body`, and `source` to a `draft_rows` list.
8. After each contact, update the progress placeholder to `(i + 1) / total` with text `Personalizing drafts... {i+1}/{total}`. Success and fallback both count as processed.
9. After the loop, clear the placeholder, then write `st.session_state.draft_result = {"error": None, "rows": draft_rows, "fallback_count": fallback_count, "total": total}`.

Render below, inside the existing `elif enrich_result["rows"]:` block, in exactly the order locked by 03-UI-SPEC section 5:
- (existing, unchanged) the `st.success("Enrichment complete — N contact(s) ready.")` banner
- the D-16 banner, conditional on `should_warn_fallback_rate(fallback_count, total)`: `st.warning("AI personalization is unavailable right now — drafts are using generic openers.", icon=":material/error:")`. Use `st.warning`, not `st.error` — drafts still exist and are usable, so this is degraded, not failed. Below the threshold, render nothing at all: no banner, no per-row indicator.
- (existing, unchanged) the `st.dataframe(table_rows)` with its Name/Company/Title/Email columns
- `st.divider()`
- `st.subheader("Email Drafts", icon=":material/drafts:")`
- one `st.expander` per draft row, in the same order as the dataframe rows, labelled exactly `View draft — {Name} ({Company})` with `icon=":material/mail:"` and `expanded=False`. Inside each expander render a single `st.code(full_text, language=None)` where `full_text` is the subject line prefixed with `Subject: `, then a blank line, then the body — one continuous block exactly as it would be sent (D-03). Do not split the AI opening line from the template body, do not bold or colour it, and do not add any caption, badge, or `draft_source` label below the code block (03-UI-SPEC section 4 locks this: rows are visually uniform regardless of whether the opener came from Claude or the fallback).

If `st.session_state.draft_result` carries an `"error"`, render it with `st.error(..., icon=":material/cancel:")` in place of the drafts section, matching the existing enrich-error branch, and still render the dataframe above it.

Add no custom CSS, no `unsafe_allow_html`, no new colour tokens, and no new page entry — D-02 keeps draft visibility on this page and the formal Review Queue is Phase 4's work. Update the module docstring to note that draft generation is chained automatically after enrichment per D-01 and that the page still never hard-stops.
  </action>
  <verify>
    <automated>python -m py_compile pages/discovery_page.py && pytest -q && test "$(grep -v '^\s*#' pages/discovery_page.py | grep -c 'build_opening_line')" -ge 2 && test "$(grep -v '^\s*#' pages/discovery_page.py | grep -c 'assemble_email')" -ge 2 && test "$(grep -v '^\s*#' pages/discovery_page.py | grep -c 'update_draft')" -ge 2 && test "$(grep -v '^\s*#' pages/discovery_page.py | grep -c 'st.stop()')" -eq 0 && test "$(grep -c 'AI personalization is unavailable right now — drafts are using generic openers.' pages/discovery_page.py)" -eq 1 && test "$(grep -c 'Personalizing drafts\.\.\.' pages/discovery_page.py)" -ge 2 && test "$(grep -c 'View draft — ' pages/discovery_page.py)" -eq 1 && test "$(grep -v '^\s*#' pages/discovery_page.py | grep -c 'unsafe_allow_html')" -eq 0 && echo WIRING_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile pages/discovery_page.py` exits 0
    - `pytest -q` exits 0 — the full suite including all Phase 1/2 tests stays green
    - `build_opening_line`, `assemble_email`, and `update_draft` each appear at least twice in non-comment lines (import plus call site)
    - `grep -v '^\s*#' pages/discovery_page.py | grep -c 'st.stop()'` returns 0 — the page never hard-stops
    - The exact D-16 copy `AI personalization is unavailable right now — drafts are using generic openers.` appears exactly once, inside an `st.warning(` call with `icon=":material/error:"`, and is guarded by `should_warn_fallback_rate(...)`
    - The progress text literal `Personalizing drafts... ` appears at least twice (initial render plus per-iteration update)
    - The expander label literal `View draft — ` appears exactly once, in an `st.expander(` call using `icon=":material/mail:"`
    - `st.divider()` and `st.subheader("Email Drafts"` both appear exactly once and sit between the `st.dataframe` call and the expander loop
    - `grep -c 'st.code(' pages/discovery_page.py` returns 1 and that call passes `language=None`
    - `grep -v '^\s*#' pages/discovery_page.py | grep -c 'unsafe_allow_html'` returns 0 — no custom CSS
    - No new `st.button` is added: `grep -c 'st.button(' pages/discovery_page.py` still returns 2 (Find Contacts, Enrich & Continue)
    - `grep -v '^\s*#' pages/discovery_page.py | grep -c 'draft_source'` returns 0 in the render section — no per-row AI-vs-fallback indicator is displayed
    - No code path derives a first name from `match.get("name")` via `.split()`
  </acceptance_criteria>
  <done>Enriching contacts automatically generates and persists a draft for every contact, shows progress while it runs, and renders a collapsed per-contact draft expander beneath the results table.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — no-hallucination sample review and draft rendering click-through</name>
  <files>pages/discovery_page.py</files>
  <read_first>
    - .planning/phases/03-ai-personalization/03-VALIDATION.md (the "Manual-Only Verifications" table — this checkpoint is the sole coverage for Phase 3 success criteria 1 and 2, which no automated test can assert)
    - .planning/phases/03-ai-personalization/03-UI-SPEC.md (Interaction Contract sections 1-5 — the visual contract being confirmed)
  </read_first>
  <what-built>
Draft generation chained automatically after "Enrich & Continue": a per-contact Claude Haiku opening line grounded only in Apollo's title and company, a shared generic fallback for sparse titles and API failures, persistence to SQLite at `status='drafted'`, a progress indicator, a per-contact "View draft" expander showing the full assembled email, and a warning banner when more than half the batch fell back.

Automated tests cover prompt grounding, both fallback triggers, template assembly, persistence, schema migration, and the banner threshold. Two things they structurally cannot cover: whether the model's real output actually stays grounded (LLM output is probabilistic), and whether the UI renders correctly (no Streamlit component test harness exists).
  </what-built>
  <action>
Pause for the developer. Do not modify code during this checkpoint. Start the app with `streamlit run app.py`, walk the steps in `<how-to-verify>` below, and wait for an explicit approval or a list of issues before marking Phase 3 complete. This checkpoint is the only coverage for Phase 3 success criteria 1 and 2 per 03-VALIDATION.md's Manual-Only Verifications table.
  </action>
  <how-to-verify>
1. Confirm a real `ANTHROPIC_API_KEY` is present in `.streamlit/secrets.toml`. If it is missing, add it before continuing — the app will refuse to boot without it.
2. Run `streamlit run app.py` and open the Contact Discovery page.
3. Pick a path, enter a company type and target role, and click "Find Contacts".
4. Click "Enrich & Continue". Confirm the progress indicator reads `Personalizing drafts... N/M` and counts up, then disappears when the loop finishes. Confirm you did NOT have to click a second button for drafts to appear.
5. Confirm the results table is unchanged (Name / Company / Title / Email), then a divider, then an "Email Drafts" heading, then one collapsed `View draft — {Name} ({Company})` expander per contact, in the same order as the table rows.
6. Expand several drafts. Confirm each shows `Subject: ...` followed by a blank line and the full body as one continuous block — the opening line should read as a natural part of the email, not visually separated or labelled.
7. **The critical check (success criterion 1 — no hallucination):** for at least 5 contacts, compare the opening line against that row's `Title` and `Company` in the table above. The opener may reference the person's title and their company name. It must NOT invent an industry, a seniority level, a location, a recent achievement, a funding round, a product name, or anything else not literally present in those two fields. Report any opener that asserts a fact you cannot trace to the title or company text.
8. Confirm the assembled body contains no leftover `[First Name]`, `[Company]`, or `{...}` placeholders, and that `[SPONSORSHIP_LINK]` still appears literally in the two sponsorship paths (this placeholder is expected — resolving it is a manual step before any real send, per D-11).
9. Confirm `sqlite3 db/outreach.db "SELECT status, draft_source, substr(opening_line,1,40) FROM prospect WHERE status='drafted' LIMIT 5;"` returns rows with `status='drafted'` and a populated `opening_line`.
10. Optional negative check: temporarily set an invalid `ANTHROPIC_API_KEY` value, rerun the enrich flow, and confirm the page shows the warning banner `AI personalization is unavailable right now — drafts are using generic openers.` with every draft still rendering a generic opener — no traceback, no frozen page, inputs still usable. Restore the real key afterwards.
  </how-to-verify>
  <verify>
    <automated>sqlite3 db/outreach.db "SELECT COUNT(*) FROM prospect WHERE status='drafted' AND opening_line IS NOT NULL;"</automated>
    <human-check>At least 5 sampled opening lines reference only the contact's title and company — no invented facts. Draft expanders render the full subject and body as one continuous block.</human-check>
  </verify>
  <acceptance_criteria>
    - The drafts section appeared with no second button click (D-01 confirmed live)
    - At least 5 opening lines were sampled and none asserts a fact outside the row's Title and Company values
    - Every expanded draft shows a single `Subject:` + body block with no unrendered merge fields
    - `sqlite3 db/outreach.db "SELECT COUNT(*) FROM prospect WHERE status='drafted' AND opening_line IS NOT NULL;"` returns a count greater than 0
    - No Streamlit traceback appeared at any point, and the page's inputs and buttons remained usable throughout
  </acceptance_criteria>
  <done>The developer confirmed at least 5 sampled opening lines are grounded in their Title/Company only, the draft expanders render correctly, and drafted rows exist in db/outreach.db — or reported specific issues to fix before the phase closes.</done>
  <resume-signal>Type "approved", or paste any opening line that referenced a fact not present in its Title/Company plus any rendering issue you saw.</resume-signal>
</task>

</tasks>

<verification>
- `pytest -q` — full suite green
- `python -m py_compile pages/discovery_page.py` exits 0
- Live click-through confirms automatic chaining, progress, expanders, and grounded openers
- `db/outreach.db` contains rows at `status='drafted'` with populated `opening_line` and `draft_source`
</verification>

<success_criteria>
- Phase 3 success criterion 1: every contact has a distinct opening line referencing only verifiable Apollo fields — confirmed by human sample review
- Phase 3 success criterion 2: the full assembled draft is visible per contact before any send action — confirmed by click-through
- Phase 3 success criterion 3: sparse enrichment data yields the safe generic opener rather than a fabrication — covered by `test_sparse_title_triggers_fallback` and the live negative check
- The page never hard-stops and never shows a traceback, even with the Anthropic API unavailable
</success_criteria>

<output>
Create `.planning/phases/03-ai-personalization/03-04-SUMMARY.md` when done.
The SUMMARY must carry a "User Setup Required" section flagging that `[SPONSORSHIP_LINK]` in `personalization/templates.py` is still a literal placeholder (D-11) — the sponsorship deck and prospectus in `tmp/` must be hosted and the placeholder replaced with a real URL before any live campaign send.
</output>
