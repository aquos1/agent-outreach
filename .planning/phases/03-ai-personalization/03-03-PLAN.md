---
phase: 03-ai-personalization
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - db/schema.py
  - db/prospects.py
autonomous: true
requirements: [PERS-01]

must_haves:
  truths:
    - "The existing populated db/outreach.db gains the opening_line, first_name, and draft_source columns on the next app boot without losing any of its existing rows"
    - "Calling ensure_schema() repeatedly is still safe — the column migration runs exactly once per column and never raises on a second call"
    - "D-04: Persisting a draft advances that contact from status='enriched' to status='drafted' and stores its opening line and draft source — written to SQLite immediately, not session-state-only"
  artifacts:
    - path: "db/schema.py"
      provides: "idempotent ALTER TABLE column migration inside ensure_schema"
      contains: "_migrate_columns"
    - path: "db/prospects.py"
      provides: "update_draft write function"
      exports: ["update_draft"]
  key_links:
    - from: "db/schema.py ensure_schema"
      to: "prospect table new columns"
      via: "ALTER TABLE ADD COLUMN guarded by duplicate-column OperationalError"
      pattern: "ALTER TABLE prospect ADD COLUMN"
    - from: "db/prospects.py update_draft"
      to: "prospect.status"
      via: "parameterized UPDATE setting status='drafted'"
      pattern: "status = 'drafted'"
---

<objective>
Extend the SQLite layer so generated drafts persist: add an idempotent column migration to `ensure_schema()` and an `update_draft()` write function that advances a contact to `status='drafted'`.

Purpose: D-04 requires drafts to live in SQLite, not session state, so Phase 4's Review Queue can query them. The live `db/outreach.db` already holds real enriched rows, so this must be an additive migration — never a drop/recreate.
Output: `db/schema.py` migrates three new columns on every boot; `db/prospects.py` gains `update_draft()`.
</objective>

## Phase Goal

**As a** teammate running an outreach campaign, **I want to** see a complete, personalized email draft for every contact I just enriched, **so that** I can trust exactly what will be sent before anything goes out.

<execution_context>
@/Users/yashpersonal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yashpersonal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-ai-personalization/03-CONTEXT.md
@.planning/phases/03-ai-personalization/03-RESEARCH.md
@.planning/phases/03-ai-personalization/03-PATTERNS.md
@.planning/phases/03-ai-personalization/03-01-SUMMARY.md

<interfaces>
<!-- Contracts this plan implements. tests written in plan 03-01 already assert against them. -->

db/schema.py:
  _NEW_COLUMNS: list[tuple[str, str]]   # [("opening_line","TEXT"), ("first_name","TEXT"), ("draft_source","TEXT")]
  _migrate_columns(conn: sqlite3.Connection) -> None
  ensure_schema(db_path: str = "db/outreach.db") -> None   # unchanged signature; now also calls _migrate_columns

db/prospects.py:
  update_draft(prospect_id: str | int, opening_line: str, source: str, first_name: str | None = None, db_path: str = "db/outreach.db") -> None

Existing prospect table columns (db/schema.py DDL — do not modify this string):
  id, name, company, company_domain, apollo_person_id, apollo_contact_id, email, path,
  status CHECK IN ('found','selected','enriched','contact_created','drafted','approved','sequenced'),
  sequence_id, created_at, updated_at
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an idempotent column migration to ensure_schema()</name>
  <files>db/schema.py</files>
  <read_first>
    - db/schema.py (whole file — the `DDL` prospect CREATE TABLE, the `VIEW_DDL` drop/recreate rationale, and `ensure_schema()`'s connect/try/finally lifecycle at lines 73-90)
    - tests/test_schema.py (`test_ensure_schema_idempotent` and the new `test_column_migration_idempotent` — the contract)
    - .planning/phases/03-ai-personalization/03-RESEARCH.md (Pitfall 1 — why editing the DDL string alone silently no-ops against the existing database)
    - .planning/phases/03-ai-personalization/03-PATTERNS.md (section "db/schema.py (migration, extend)")
  </read_first>
  <action>
Add a module-level `_NEW_COLUMNS` list of `(name, coltype)` pairs: `("opening_line", "TEXT")`, `("first_name", "TEXT")`, and `("draft_source", "TEXT")` with a trailing comment on the last noting its two valid values `'ai'` and `'fallback'`.

Add a private `_migrate_columns(conn)` helper that loops over `_NEW_COLUMNS` and executes `ALTER TABLE prospect ADD COLUMN {name} {coltype}` for each, wrapped in `try/except sqlite3.OperationalError`. Swallow the exception ONLY when `"duplicate column name"` appears in `str(e)`; re-raise anything else. Column names and types come exclusively from the hardcoded `_NEW_COLUMNS` constant — no caller-supplied or Apollo-derived value is ever interpolated into DDL.

Call `_migrate_columns(conn)` from inside `ensure_schema()`'s existing `try:` block, after `conn.executescript(VIEW_DDL)` and before `conn.commit()`, so the existing connect/try/finally lifecycle and the single commit are preserved.

Do NOT modify the `DDL` string's `CREATE TABLE IF NOT EXISTS prospect (...)` statement. `CREATE TABLE IF NOT EXISTS` is a no-op against the already-populated `db/outreach.db`, so a DDL edit would silently never add the columns (Pitfall 1). Do NOT drop or recreate the `prospect` table — it currently holds real enriched rows.

Update the module docstring to note that `ensure_schema()` also applies additive column migrations to an existing `prospect` table, keeping the idempotent-boot guarantee.

After the edit, run `ensure_schema()` once against the real `db/outreach.db` so the live development database is migrated in place.
  </action>
  <verify>
    <automated>pytest tests/test_schema.py -q && python -c "from db.schema import ensure_schema; ensure_schema(); ensure_schema()" && python -c "import sqlite3;c=sqlite3.connect('db/outreach.db');cols={r[1] for r in c.execute('PRAGMA table_info(prospect)')};n=c.execute('SELECT COUNT(*) FROM prospect').fetchone()[0];assert {'opening_line','first_name','draft_source'} <= cols, cols;assert n > 0, 'existing rows lost';print('MIGRATION_OK rows=',n)"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_schema.py -q` exits 0, including `test_column_migration_idempotent` and the pre-existing `test_ensure_schema_idempotent`
    - Calling `ensure_schema()` twice in the same process raises nothing
    - `PRAGMA table_info(prospect)` on the real `db/outreach.db` lists `opening_line`, `first_name`, and `draft_source`
    - `SELECT COUNT(*) FROM prospect` on the real `db/outreach.db` is greater than 0 — pre-existing rows survived the migration
    - `git diff db/schema.py` shows no change to the `CREATE TABLE IF NOT EXISTS prospect` statement inside `DDL` and no `DROP TABLE`
    - `grep -v '^\s*#' db/schema.py | grep -c 'DROP TABLE'` returns 0
    - `grep -v '^\s*#' db/schema.py | grep -c 'duplicate column name'` returns 1 — the exception filter is present in executable code
  </acceptance_criteria>
  <done>ensure_schema() adds the three draft columns to any pre-existing prospect table exactly once, is safe on every boot, and preserves all existing rows.</done>
</task>

<task type="auto">
  <name>Task 2: Add update_draft() to db/prospects.py</name>
  <files>db/prospects.py</files>
  <read_first>
    - db/prospects.py (module docstring lines 1-12 stating the `?`-placeholder-only security rule; `insert_enriched` lines 83-129 — the connect/try/finally + parameterized-write shape and the `apollo_person_id` storage convention)
    - db/schema.py (the prospect table's status CHECK constraint — 'drafted' is already a permitted value)
    - tests/test_prospects.py (`test_update_draft_writes_status_drafted` — the contract)
    - .planning/phases/03-ai-personalization/03-RESEARCH.md (Code Examples section, `db/prospects.py` draft-write function; Pitfall 2 on first_name)
    - .planning/phases/03-ai-personalization/03-PATTERNS.md (section "db/prospects.py (model/service, extend)")
  </read_first>
  <action>
Add `update_draft(prospect_id, opening_line, source, first_name=None, db_path="db/outreach.db") -> None` following `insert_enriched`'s exact shape: `conn = sqlite3.connect(db_path)`, a `try:` block ending in `conn.commit()`, and a `finally: conn.close()`.

The statement is a single parameterized UPDATE setting `opening_line = ?`, `draft_source = ?`, `first_name = ?`, `status = 'drafted'`, and `updated_at = CURRENT_TIMESTAMP`, with `WHERE apollo_person_id = ?`. Use `?` placeholders exclusively — never f-string, `.format()`, or `%` interpolation of any Apollo-derived or AI-generated value into SQL (Phase 1 security rule, still in force per 03-RESEARCH.md's Anti-Patterns).

`apollo_person_id` — not the internal `id` primary key — is the correct WHERE key, because that is what `insert_enriched` stores and what the raw Apollo match dict's `id` field carries in the caller's loop.

`first_name` is written because the `prospect` table only ever stored a combined `name` string; Phase 4 must not have to re-derive a first name by splitting it, which breaks on compound names (Pitfall 2). When the caller passes `None`, write `None` rather than skipping the column.

Docstring: one-line summary plus a rationale line citing D-04 (drafts persist to SQLite with `status='drafted'`, not session state only) and noting `apollo_person_id` as the match key.

Do not add a read/query function in this plan — Phase 4's Review Queue owns draft reads.
  </action>
  <verify>
    <automated>pytest tests/test_prospects.py -q && test "$(grep -v '^\s*#' db/prospects.py | grep -c \"status = 'drafted'\")" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_prospects.py -q` exits 0, including `test_update_draft_writes_status_drafted` and all pre-existing tests
    - After `update_draft("apollo-1", "A grounded opener.", "ai", first_name="Jamie", db_path=tmp)`, a direct SQL SELECT on that row returns `opening_line="A grounded opener."`, `draft_source="ai"`, `first_name="Jamie"`, `status="drafted"`
    - Rows with a different `apollo_person_id` are unmodified by the call
    - `grep -v '^\s*#' db/prospects.py | grep -c "status = 'drafted'"` returns 1
    - The `update_draft` body contains no f-string, `.format(`, or `%` formatting applied to a SQL string — every value is bound through a `?` placeholder
    - `python -c "from db.prospects import update_draft; import inspect; print(list(inspect.signature(update_draft).parameters))"` prints `['prospect_id', 'opening_line', 'source', 'first_name', 'db_path']`
  </acceptance_criteria>
  <done>update_draft persists an opening line and its source and advances the contact to status='drafted' using parameterized SQL only.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_schema.py tests/test_prospects.py -q` — green
- `pytest -q` — full suite green, no Phase 1/2 regressions
- The real `db/outreach.db` has the three new columns and has lost no rows
</verification>

<success_criteria>
- `ensure_schema()` is still idempotent and now migrates `opening_line`, `first_name`, and `draft_source` onto a pre-existing prospect table
- `update_draft()` writes all four fields and advances status to 'drafted' via parameterized SQL
- No existing prospect row was dropped or recreated
</success_criteria>

<output>
Create `.planning/phases/03-ai-personalization/03-03-SUMMARY.md` when done.
</output>
